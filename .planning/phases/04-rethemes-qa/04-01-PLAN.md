---
phase: 04-rethemes-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/mentions-legales/page.tsx
autonomous: true
requirements:
  - THEME-01
  - QA-03

must_haves:
  truths:
    - "/mentions-legales renders with dark bg from CSS var, no legacy hex"
    - "All headings use var(--ink) — no text-white or indigo color classes remain"
    - "All links use var(--acid) — no text-indigo-400 or text-indigo-300 remain"
    - "Legal text content is word-for-word identical to the pre-retheme version"
    - "metadata title is 'Mentions Légales | BRICON ANATHOLY' — no freelance signals"
    - "Qualité field reads 'Agence web & IA — Selenium Phase 02'"
  artifacts:
    - path: "src/app/mentions-legales/page.tsx"
      provides: "Rethemed legal page — CSS vars only, preserved content"
      contains: "bg-[var(--bg)]"
  key_links:
    - from: "src/app/mentions-legales/page.tsx"
      to: "src/app/globals.css"
      via: "CSS var classes (bg-[var(--bg)], text-[var(--ink)], etc.)"
      pattern: "var\\(--"
---

<objective>
Apply the Selenium Phase 02 design system to `/mentions-legales` via in-place class substitution. All legacy hardcoded hex and indigo/gray Tailwind classes are replaced with CSS vars. Legal text content is preserved word-for-word. Metadata is updated to agency positioning (QA-03).

Purpose: THEME-01 — every page uses the new design system before v1.0 ships.
Output: `src/app/mentions-legales/page.tsx` rethemed with CSS vars, updated metadata, no legacy color classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rethemes-qa/04-CONTEXT.md
@.planning/phases/04-rethemes-qa/04-UI-SPEC.md

<interfaces>
<!-- CSS vars canonical source — globals.css :root (lines 4-16) -->
```css
:root {
  --bg:        #0A0B0C;
  --acid:      #C4F542;
  --ink:       #ECEAE3;
  --warm:      #E07856;
  --line:      #1F1F1F;
  --bg-2:      #111213;
  --ink-dim:   #9A9690;
  --ink-faint: #5A5751;
}
```

<!-- Font cascade — layout.tsx body class provides Manrope as default body font,
     Bricolage Grotesque is accessible via var(--font-display) or font-bricolage utility.
     No per-component font import needed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply class substitution map to mentions-légales page</name>
  <files>src/app/mentions-legales/page.tsx</files>

  <read_first>
    - src/app/mentions-legales/page.tsx — MUST read current state before editing (preserve legal text word-for-word)
    - src/app/globals.css — confirm var names and font vars before touching classes
  </read_first>

  <action>
Edit `src/app/mentions-legales/page.tsx` using in-place class substitution. Apply every replacement from the table below. Do NOT rewrite the file from scratch. Legal text (all French legal paragraphs) must be identical before and after — only className strings change.

**Full class substitution map (apply all):**

| Location | Current class(es) | Replace with |
|---|---|---|
| `<main>` (line 26) | `bg-[#030311]` | `bg-[var(--bg)]` |
| mono label `<p>` (line 30) | `text-indigo-400/70` | `text-[var(--acid)]/70` |
| h1 `<h1>` (line 33) | `text-transparent bg-clip-text bg-linear-to-r from-indigo-400 to-purple-400` | `text-[var(--ink)]` (remove gradient classes entirely — flat typography) |
| subtitle `<p>` (line 36) | `text-gray-500` | `text-[var(--ink-faint)]` |
| Section h2 (line 13, inside Section component) | `text-white` | `text-[var(--ink)]` |
| Section h2 (line 13) | `border-indigo-500/30` | `border-[var(--line)]` |
| Section body div (line 16) | `text-gray-400` | `text-[var(--ink-dim)]` |
| All `<span className="text-gray-300 font-medium">` spans (lines 43, 44, 45, 47, 55, 62, 103, 106) | `text-gray-300` | `text-[var(--ink)]` |
| All link `<a>` elements (lines 50, 70, 112, 122) | `text-indigo-400 hover:text-indigo-300` | `text-[var(--acid)] hover:text-[var(--acid)]/80` |
| timestamp `<p>` (line 168) | `text-gray-600` | `text-[var(--ink-faint)]` |

**h1 exact before → after:**
```tsx
// BEFORE
<h1 className="text-4xl font-bold text-transparent bg-clip-text bg-linear-to-r from-indigo-400 to-purple-400">

// AFTER
<h1 className="text-4xl font-bold text-[var(--ink)]">
```

**Section component h2 exact before → after:**
```tsx
// BEFORE
<h2 className="text-xl font-bold text-white mb-4 pb-2 border-b border-indigo-500/30">

// AFTER
<h2 className="text-xl font-bold text-[var(--ink)] mb-4 pb-2 border-b border-[var(--line)]">
```

**Qualité span + content exact before → after (line 44):**
```tsx
// BEFORE
<p><span className="text-gray-300 font-medium">Qualité :</span> Entrepreneur individuel — Développeur web & IA freelance</p>

// AFTER
<p><span className="text-[var(--ink)] font-medium">Qualité :</span> Agence web & IA — Selenium Phase 02</p>
```

Note: The `font-bold` on h1 and h2 may stay — Bricolage Grotesque cascades from layout.tsx body class automatically. No explicit font-family declaration needed.
  </action>

  <verify>
    <automated>
      grep -n "bg-\[#030311\]\|text-indigo-400\|text-indigo-300\|text-white\|text-gray-400\|text-gray-300\|text-gray-500\|text-gray-600\|border-indigo-500\|bg-linear-to-r\|bg-clip-text\|from-indigo\|to-purple" src/app/mentions-legales/page.tsx
    </automated>
  </verify>

  <done>
    - Command above returns zero matches (no legacy color classes remain)
    - `grep "bg-\[var(--bg)\]" src/app/mentions-legales/page.tsx` matches line 26
    - `grep "text-\[var(--acid)\]" src/app/mentions-legales/page.tsx` matches at least 4 link occurrences
    - `grep "border-\[var(--line)\]" src/app/mentions-legales/page.tsx` matches Section h2
    - `grep "text-\[var(--ink-faint)\]" src/app/mentions-legales/page.tsx` matches subtitle and timestamp
    - `grep "Agence web" src/app/mentions-legales/page.tsx` matches (Qualité field updated)
    - Legal text paragraphs (LCEN reference, RGPD text, cookie text) are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Update metadata to agency positioning</name>
  <files>src/app/mentions-legales/page.tsx</files>

  <read_first>
    - src/app/mentions-legales/page.tsx — read current metadata block (lines 5-9) before editing
  </read_first>

  <action>
Update the `metadata` export at the top of `src/app/mentions-legales/page.tsx` (lines 5-9).

**Exact before → after:**
```tsx
// BEFORE
export const metadata: Metadata = {
  title: 'Mentions Légales | Anatholy BRICON',
  description: 'Mentions légales du site personnel de Anatholy BRICON, développeur Full Stack & ingénieur IA freelance basé à Tours, France.',
  robots: { index: true, follow: true },
};

// AFTER
export const metadata: Metadata = {
  title: 'Mentions Légales | BRICON ANATHOLY',
  description: 'Mentions légales de Selenium Phase 02, agence web & IA basée à Tours, France.',
  robots: { index: true, follow: true },
};
```

No other changes in this task — only lines 6 and 7 change.
  </action>

  <verify>
    <automated>
      grep -n "title\|description\|freelance\|Anatholy BRICON\|développeur" src/app/mentions-legales/page.tsx | head -10
    </automated>
  </verify>

  <done>
    - `grep "title:" src/app/mentions-legales/page.tsx` returns `'Mentions Légales | BRICON ANATHOLY'`
    - `grep "freelance\|développeur Full Stack\|site personnel" src/app/mentions-legales/page.tsx` returns zero matches in the metadata block (the word "freelance" must NOT appear in title or description)
    - `grep "Selenium Phase 02" src/app/mentions-legales/page.tsx` matches the description field
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static page → search engine crawlers | metadata fields are consumed directly by crawlers — no user input, no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | metadata description | mitigate | Remove freelance/developer keywords per D-02 — done in Task 2 |
| T-04-01-02 | Tampering | legal content | accept | Content is static, server-rendered, no user input path; word-for-word preservation enforced by acceptance criteria |
</threat_model>

<verification>
After both tasks:
1. `grep -rn "bg-\[#030311\]\|text-indigo-400\|text-indigo-300\|border-indigo-500\|text-white\|text-gray-[3456]" src/app/mentions-legales/page.tsx` — must return zero matches
2. `grep "BRICON ANATHOLY" src/app/mentions-legales/page.tsx` — must match metadata title
3. `grep "freelance\|Développeur web" src/app/mentions-legales/page.tsx` — must return zero matches in metadata lines; the word may only appear in legal text sections if it was pre-existing there (check against original)
4. `grep -c "var(--" src/app/mentions-legales/page.tsx` — must return ≥ 8 (all replaced classes use CSS vars)
</verification>

<success_criteria>
- `src/app/mentions-legales/page.tsx` contains zero occurrences of: `bg-[#030311]`, `text-indigo-400`, `text-indigo-300`, `border-indigo-500`, `text-white`, `text-gray-300`, `text-gray-400`, `text-gray-500`, `text-gray-600`, `bg-linear-to-r`, `from-indigo-400`, `to-purple-400`
- All color classes use CSS vars: `var(--bg)`, `var(--ink)`, `var(--acid)`, `var(--line)`, `var(--ink-dim)`, `var(--ink-faint)`
- Metadata title === `'Mentions Légales | BRICON ANATHOLY'`
- Qualité field reads `'Agence web & IA — Selenium Phase 02'`
- Legal text sections (LCEN, RGPD, Cookies, Propriété intellectuelle, etc.) are word-for-word identical to pre-edit content
</success_criteria>

<output>
After completion, create `.planning/phases/04-rethemes-qa/04-01-SUMMARY.md`
</output>
