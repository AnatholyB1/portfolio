---
phase: 04-rethemes-qa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/demo/page.tsx
autonomous: true
requirements:
  - THEME-02

must_haves:
  truths:
    - "/demo renders with bg-[var(--bg)] — no #0f172a legacy hex"
    - "Phone number block has acid-tinted CTA style (bg-[var(--acid)]/10 border-[var(--acid)]/20)"
    - "Phone number text uses var(--acid) — primary CTA accent"
    - "Orders count stat uses var(--acid); products count stays text-emerald-400"
    - "All panel/card backgrounds use var(--bg-2) — no #1e293b remains"
    - "All Supabase Realtime wiring unchanged — channels, subscriptions, fetchOrders, fetchProducts, newOrderId flash"
    - "Semantic stock colors (text-red-400, text-amber-400, text-emerald-400) are untouched"
    - "En ligne badge and new-order highlight animation classes are untouched"
  artifacts:
    - path: "src/app/demo/page.tsx"
      provides: "Rethemed CRM dashboard — CSS vars only, all Supabase logic preserved"
      contains: "bg-[var(--bg)]"
  key_links:
    - from: "src/app/demo/page.tsx"
      to: "src/app/globals.css"
      via: "CSS var classes (bg-[var(--bg)], bg-[var(--bg-2)], text-[var(--acid)], etc.)"
      pattern: "var\\(--"
    - from: "src/app/demo/page.tsx"
      to: "supabase channels (demo-orders, demo-products)"
      via: "useEffect with channel subscriptions"
      pattern: "supabase\\.channel"
---

<objective>
Apply the Selenium Phase 02 design system to `/demo` via in-place class substitution. Background, panel, text, and border classes are replaced with CSS vars. The phone number CTA block receives an acid-tinted panel style. All Supabase Realtime logic is preserved byte-for-byte. Semantic UX colors (stock indicators, live badge, new-order flash) are not touched.

Purpose: THEME-02 — the CRM demo page uses the new brand without breaking any real-time functionality.
Output: `src/app/demo/page.tsx` rethemed with CSS vars; all Supabase hooks, channels, state, and fetch functions identical to before.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rethemes-qa/04-CONTEXT.md
@.planning/phases/04-rethemes-qa/04-UI-SPEC.md

<interfaces>
<!-- CSS vars canonical source — globals.css :root (lines 4-16) -->
```css
:root {
  --bg:        #0A0B0C;   /* page background */
  --bg-2:      #111213;   /* card / panel background */
  --acid:      #C4F542;   /* brand accent / CTA */
  --ink:       #ECEAE3;   /* primary text */
  --line:      #1F1F1F;   /* borders / dividers */
  --ink-dim:   #9A9690;   /* secondary text */
  --ink-faint: #5A5751;   /* tertiary text / timestamps */
}
```

<!-- PRESERVE UNCHANGED (D-04, D-07) — do not touch these classes anywhere in demo/page.tsx:
  text-red-400       — stockColor() out-of-stock
  text-amber-400     — stockColor() low-stock
  text-emerald-400   — stockColor() available, products count, order status checkmark
  bg-emerald-500/20  — "En ligne" badge background
  border-emerald-500/30 — "En ligne" badge border
  bg-emerald-400     — "En ligne" pulse dot
  bg-emerald-500/10 border-l-2 border-emerald-500 — new order highlight animation
-->

<!-- PRESERVE UNCHANGED — all Supabase wiring (lines 38-95):
  useEffect, ordersChannel, productsChannel, fetchOrders(), fetchProducts(),
  stockColor(), stockEmoji(), newOrderId state, setTimeout flash
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply class substitution map to demo CRM page</name>
  <files>src/app/demo/page.tsx</files>

  <read_first>
    - src/app/demo/page.tsx — MUST read current state before editing; identify every legacy class occurrence before touching anything
    - src/app/globals.css — confirm var names before substituting
  </read_first>

  <action>
Edit `src/app/demo/page.tsx` using in-place class substitution. Apply every replacement from the table below. Do NOT rewrite the file from scratch. All code from line 1 through line 95 (interfaces, state, useEffect, fetchOrders, fetchProducts, stockColor, stockEmoji) must be byte-identical before and after.

**Full class substitution map (apply all — in-place only):**

| Current class | Replace with | Notes |
|---|---|---|
| `bg-[#0f172a]` (outer div, line 107) | `bg-[var(--bg)]` | Page background |
| `bg-[#1e293b]` (phone block, line 117) | `bg-[var(--acid)]/10` | Acid-tinted CTA panel (D-03, D-05) |
| `border border-indigo-500/30` (phone block, line 117) | `border border-[var(--acid)]/20` | Acid border on phone CTA (D-03) |
| `text-gray-400` (phone label, line 118) | `text-[var(--ink-dim)]` | Secondary label text |
| `text-indigo-400` (phone number, line 121) | `text-[var(--acid)]` | Primary CTA accent (D-05) |
| `text-gray-500` (phone sub-label, line 124) | `text-[var(--ink-faint)]` | Tertiary label text |
| `bg-[#1e293b] border border-gray-800` (orders stat card, line 130) | `bg-[var(--bg-2)] border border-[var(--line)]` | Panel background |
| `text-blue-400` (orders count, line 131) | `text-[var(--acid)]` | Orders count accent (D-06) |
| `text-gray-500` (orders stat label, line 132) | `text-[var(--ink-faint)]` | Tertiary label |
| `bg-[#1e293b] border border-gray-800` (products stat card, line 134) | `bg-[var(--bg-2)] border border-[var(--line)]` | Panel background |
| `text-gray-500` (products stat label, line 138) | `text-[var(--ink-faint)]` | Tertiary label |
| `text-xs text-gray-400 uppercase tracking-widest` (orders section label, line 144) | `text-xs text-[var(--ink-dim)] uppercase tracking-widest` | Section label |
| `bg-[#1e293b] border border-gray-800` (orders list panel, line 147) | `bg-[var(--bg-2)] border border-[var(--line)]` | Panel background |
| `text-gray-600` (empty state text, line 149) | `text-[var(--ink-faint)]` | Empty state |
| `divide-gray-800/60` (orders divider, line 153) | `divide-[var(--line)]/60` | List divider |
| `text-white font-medium` (customer name, line 165) | `text-[var(--ink)] font-medium` | Primary text |
| `text-gray-400` (order items list, line 169) | `text-[var(--ink-dim)]` | Secondary text |
| `text-gray-600` (order timestamp, line 173) | `text-[var(--ink-faint)]` | Tertiary/timestamp |
| `text-xs text-gray-400 uppercase tracking-widest` (Stock label, line 192) | `text-xs text-[var(--ink-dim)] uppercase tracking-widest` | Section label |
| `bg-[#1e293b] border border-gray-800` (stock panel, line 193) | `bg-[var(--bg-2)] border border-[var(--line)]` | Panel background |
| `divide-gray-800/60` (stock divider, line 194) | `divide-[var(--line)]/60` | List divider |
| `text-white` (product name, line 198) | `text-[var(--ink)]` | Primary text |
| `text-gray-600` (product category, line 199) | `text-[var(--ink-faint)]` | Tertiary label |
| `text-gray-700` (footer credit, line 211) | `text-[var(--ink-faint)]` | Footer text |

**DO NOT replace these (D-04, D-07):**
- `text-red-400`, `text-amber-400`, `text-emerald-400` anywhere in the file
- `bg-emerald-500/20`, `border-emerald-500/30`, `bg-emerald-400` (En ligne badge)
- `bg-emerald-500/10 border-l-2 border-emerald-500` (new order highlight animation)

**Note on line 180 (`text-white text-sm font-medium` — order total):** Replace `text-white` with `text-[var(--ink)]`.

After editing, verify that lines 1-95 (Supabase interfaces, state, useEffect, fetchOrders, fetchProducts, stockColor, stockEmoji) are identical to the pre-edit content.
  </action>

  <verify>
    <automated>
      grep -n "bg-\[#0f172a\]\|bg-\[#1e293b\]\|border-indigo-500\|text-indigo-400\|text-blue-400\|text-gray-400\|text-gray-500\|text-gray-600\|text-gray-700\|text-gray-800\|border-gray-800\|divide-gray-800\|text-white" src/app/demo/page.tsx
    </automated>
  </verify>

  <done>
    - Command above returns zero matches (all legacy color classes replaced)
    - `grep "bg-\[var(--bg)\]" src/app/demo/page.tsx` matches (outer div)
    - `grep "bg-\[var(--acid)\]/10" src/app/demo/page.tsx` matches (phone block background)
    - `grep "border-\[var(--acid)\]/20" src/app/demo/page.tsx` matches (phone block border)
    - `grep "text-\[var(--acid)\]" src/app/demo/page.tsx` returns at least 2 matches (phone number + orders count)
    - `grep "bg-\[var(--bg-2)\]" src/app/demo/page.tsx` returns at least 4 matches (stat cards + panels)
    - `grep "text-red-400\|text-amber-400\|text-emerald-400" src/app/demo/page.tsx` returns matches (semantic colors preserved)
    - `grep "bg-emerald-500/20\|border-l-2 border-emerald-500" src/app/demo/page.tsx` returns matches (live badge and order flash preserved)
    - `grep "supabase.channel\|fetchOrders\|fetchProducts\|newOrderId" src/app/demo/page.tsx` returns matches (Supabase wiring unchanged)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Realtime → client state | External data flows into React state via channel subscriptions — must remain fully intact after retheme |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | Supabase channel subscriptions | mitigate | Acceptance criteria require grep matches on supabase.channel, fetchOrders, fetchProducts — confirms wiring is untouched |
| T-04-02-02 | Information Disclosure | CRM order data (customer names, totals) | accept | Demo data — Supabase RLS manages access; no change to auth or data access in this plan |
</threat_model>

<verification>
After task completion:
1. `grep -n "bg-\[#0f172a\]\|bg-\[#1e293b\]\|text-indigo-400\|text-blue-400\|border-indigo-500\|text-gray-[4-9]\|border-gray-8\|divide-gray-8\|text-white" src/app/demo/page.tsx` — must return zero matches
2. `grep -c "var(--" src/app/demo/page.tsx` — must return ≥ 15 (all replaced classes use CSS vars)
3. `grep "text-red-400\|text-amber-400\|text-emerald-400\|bg-emerald-500" src/app/demo/page.tsx` — must return ≥ 5 matches (semantic colors preserved)
4. `grep "supabase.channel('demo-orders')\|supabase.channel('demo-products')" src/app/demo/page.tsx` — must return 2 matches (channels untouched)
</verification>

<success_criteria>
- `src/app/demo/page.tsx` contains zero occurrences of: `bg-[#0f172a]`, `bg-[#1e293b]`, `text-indigo-400`, `text-blue-400`, `border-indigo-500`, `text-white`, `text-gray-400`, `text-gray-500`, `text-gray-600`, `text-gray-700`, `border-gray-800`, `divide-gray-800`
- Phone CTA block uses `bg-[var(--acid)]/10 border border-[var(--acid)]/20` with `text-[var(--acid)]` on the phone number
- Orders count uses `text-[var(--acid)]`; products count retains `text-emerald-400`
- All panel/card backgrounds use `bg-[var(--bg-2)]` with `border-[var(--line)]`
- Supabase channel subscriptions (demo-orders, demo-products), fetchOrders, fetchProducts, stockColor, stockEmoji, newOrderId are byte-identical to pre-edit
- Semantic stock colors and live-status classes are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/04-rethemes-qa/04-02-SUMMARY.md`
</output>
