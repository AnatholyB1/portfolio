---
phase: 04-rethemes-qa
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - .planning/phases/04-rethemes-qa/04-QA-CHECKLIST.md
  - .planning/phases/04-rethemes-qa/VERIFICATION.md
autonomous: false
requirements:
  - QA-01
  - QA-02
  - QA-03
  - QA-04

must_haves:
  truths:
    - "A written QA checklist exists with specific testable items per requirement (QA-01 through QA-04)"
    - "QA-04 feuillette git diff result is documented — pass or fail explicitly stated"
    - "All pages have SEO metadata checked against freelance keyword list"
    - "prefers-reduced-motion code check result documented per component"
    - "Human has verified checklist items manually before phase is marked complete"
  artifacts:
    - path: ".planning/phases/04-rethemes-qa/04-QA-CHECKLIST.md"
      provides: "Structured checklist with one item per page/requirement combination"
      contains: "QA-01"
    - path: ".planning/phases/04-rethemes-qa/VERIFICATION.md"
      provides: "QA-04 feuillette diff result + automated check results"
      contains: "QA-04"
  key_links:
    - from: ".planning/phases/04-rethemes-qa/VERIFICATION.md"
      to: "src/app/demo/feuillette/"
      via: "git diff <baseline-commit> HEAD -- src/app/demo/feuillette/"
      pattern: "feuillette"
---

<objective>
Produce the QA checklist file and run the automated QA-04 feuillette diff check. Create VERIFICATION.md with results. Checkpoint for human to verify responsive layout (QA-01), reduced-motion (QA-02), and SEO metadata (QA-03) manually using the checklist.

Purpose: QA-01 through QA-04 — integration quality gate before v1.0 ships.
Output: `04-QA-CHECKLIST.md` with all checklist items, `VERIFICATION.md` with QA-04 diff result, human checkpoint to verify the rest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rethemes-qa/04-CONTEXT.md
@.planning/phases/04-rethemes-qa/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run QA-04 feuillette diff and produce QA checklist + VERIFICATION.md</name>
  <files>
    .planning/phases/04-rethemes-qa/04-QA-CHECKLIST.md
    .planning/phases/04-rethemes-qa/VERIFICATION.md
  </files>

  <read_first>
    - .planning/phases/04-rethemes-qa/04-CONTEXT.md — D-11, D-12 for checklist scope and feuillette diff instructions
    - .planning/phases/04-rethemes-qa/04-UI-SPEC.md — QA Checklist Scope table for item definitions
  </read_first>

  <action>
**Step A — QA-04 feuillette git diff:**

Run: `rtk git log --oneline` to identify the first commit of the milestone (pre-Phase-1 baseline — the commit before any CSS token changes). Then run:
```bash
git diff <baseline-commit> HEAD -- src/app/demo/feuillette/
```
Record whether output is empty (PASS) or non-empty (FAIL). If FAIL, list the changed files.

**Step B — SEO metadata code check (QA-03):**

Run these grep checks and record output:
```bash
grep -rn "freelance\|Développeur web\|hire.developer\|développeur Full Stack\|site personnel\|Anatholy BRICON" \
  src/app/layout.tsx src/app/page.tsx src/app/services/page.tsx src/app/mentions-legales/page.tsx src/app/demo/page.tsx
```
Record matches. Any match in a `metadata` export block = QA-03 FAIL for that page.

**Step C — prefers-reduced-motion code check (QA-02):**

Run:
```bash
grep -rn "prefers-reduced-motion\|reducedMotion\|prefersReducedMotion" \
  src/components/CinemaIntro.tsx src/components/layout/CustomCursor.tsx \
  src/hooks/useReveals.ts src/components/sections/PhoneAgentSection.tsx \
  src/components/sections/MethodologySection.tsx 2>/dev/null
```
Record which files handle the media query and which don't.

**Step D — Create `04-QA-CHECKLIST.md`:**

```markdown
# Phase 4 QA Checklist

Generated: 2026-05-18
Requirement: QA-01, QA-02, QA-03, QA-04

---

## QA-01 — Responsive at 900px

For each page, set browser viewport to exactly 900px wide and verify:

### Landing Page (`/`)
- [ ] No horizontal scroll at 900px
- [ ] Navbar collapses correctly (hamburger or stacked links — no overflow)
- [ ] Hero section title does not overflow horizontally
- [ ] Stat grid remains readable (no clipped numbers)
- [ ] All sections visible without horizontal scroll

### Services Page (`/services`)
- [ ] No horizontal scroll at 900px
- [ ] Pricing packs grid: wraps cleanly (no cut-off cards)
- [ ] Methodology section rail visible and scrollable
- [ ] Options grid: wraps cleanly

### Mentions Légales (`/mentions-legales`)
- [ ] No horizontal scroll at 900px
- [ ] Legal text readable (no overflow)
- [ ] Navbar collapses correctly

### Demo (`/demo`)
- [ ] No horizontal scroll at 900px
- [ ] Stat grid (2 columns) remains visible
- [ ] Orders + Stock panels stack cleanly on narrow viewport

---

## QA-02 — prefers-reduced-motion

**Code check results:** [FILL FROM STEP C GREP OUTPUT]

Manual verification steps:
1. Open DevTools → Rendering tab → check "Emulate CSS media feature prefers-reduced-motion: reduce"
2. Hard-reload the page (Cmd/Ctrl+Shift+R)

### Cinema Intro
- [ ] With reduced-motion: overlay skips animation and dismisses immediately (or appears already dismissed)
- [ ] localStorage `cinemaPlayed` flag still set correctly

### Scroll Reveal ([data-reveal] elements)
- [ ] With reduced-motion: elements appear immediately on load without fade/slide animation

### PhoneAgent section
- [ ] With reduced-motion: SVG particle flow animation absent or static
- [ ] Pinned scroll behavior still functional (no animation, but scroll still works)

### Methodology section
- [ ] With reduced-motion: GSAP ScrollTrigger rail does not animate; steps visible without motion

---

## QA-03 — SEO Metadata

**Code check results:** [FILL FROM STEP B GREP OUTPUT]

For each page, confirm in browser (View Source or DevTools → Elements → `<head>`):

### Landing Page (`/`)
- [ ] `<title>` contains no "freelance", "hire", "développeur" keywords
- [ ] `<meta name="description">` contains no freelance/hire-developer signals

### Services Page (`/services`)
- [ ] `<title>` contains no freelance signals
- [ ] `<meta name="description">` positioned as agency offer

### Mentions Légales (`/mentions-legales`)
- [ ] `<title>` is exactly: `Mentions Légales | BRICON ANATHOLY`
- [ ] `<meta name="description">` reads: "Mentions légales de Selenium Phase 02, agence web & IA basée à Tours, France."

### Demo (`/demo`)
- [ ] `<title>` contains no freelance signals (note: demo may have minimal metadata — verify what's set)

---

## QA-04 — /demo/feuillette Untouched

**Automated diff result:** [FILL FROM STEP A GIT DIFF OUTPUT]

- [ ] `git diff <baseline> HEAD -- src/app/demo/feuillette/` output is empty
- [ ] Diff result: PASS / FAIL — document below

**Result:** [PASS or FAIL]
**Baseline commit used:** [commit hash]
**Changed files (if any):** [list or "none"]

---

## Overall QA Status

| Requirement | Status | Notes |
|-------------|--------|-------|
| QA-01 (900px responsive) | [ ] Pending human verify | — |
| QA-02 (prefers-reduced-motion) | [ ] Pending human verify | — |
| QA-03 (SEO metadata) | [ ] Code check + human verify | — |
| QA-04 (feuillette untouched) | [FILL] | Automated git diff |
```

**Step E — Create `VERIFICATION.md`** with the raw automated check outputs:
- QA-04 git diff command run, commit hash used, diff output (or "empty — PASS")
- QA-03 grep output (or "zero matches — PASS")
- QA-02 grep output per component
  </action>

  <verify>
    <automated>
      test -f .planning/phases/04-rethemes-qa/04-QA-CHECKLIST.md && test -f .planning/phases/04-rethemes-qa/VERIFICATION.md && grep -c "QA-04" .planning/phases/04-rethemes-qa/VERIFICATION.md
    </automated>
  </verify>

  <done>
    - `04-QA-CHECKLIST.md` exists with sections for QA-01, QA-02, QA-03, QA-04
    - `VERIFICATION.md` exists with QA-04 git diff result documented (PASS or FAIL explicitly stated)
    - `VERIFICATION.md` contains the baseline commit hash used for the diff
    - `grep "QA-03 grep" VERIFICATION.md` or equivalent shows SEO grep output recorded
    - All `[FILL FROM STEP ...]` placeholders in the checklist are replaced with actual results from Step A/B/C
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of QA-01, QA-02, QA-03 checklist items</name>

  <what-built>
    - `04-QA-CHECKLIST.md` with all testable items for QA-01 (responsive 900px), QA-02 (prefers-reduced-motion), and QA-03 (SEO metadata)
    - `VERIFICATION.md` with QA-04 feuillette diff result and SEO grep output already filled in
  </what-built>

  <how-to-verify>
1. Run `npm run dev` (or `rtk npm run dev`) to start the dev server at http://localhost:3000

2. **QA-01 — 900px responsive:** Open DevTools, set viewport to 900px. Visit `/`, `/services`, `/mentions-legales`, `/demo` in sequence. Check each item in the QA-01 section of `04-QA-CHECKLIST.md`. Look for: horizontal scroll, nav overflow, grid overflow.

3. **QA-02 — prefers-reduced-motion:** In DevTools → Rendering tab, enable "Emulate CSS media feature prefers-reduced-motion: reduce". Hard-reload each page. Check: cinema intro skips motion, scroll-reveal elements appear immediately, PhoneAgent animation absent or static, Methodology rail no motion.

4. **QA-03 — SEO metadata:** For `/mentions-legales`: View Source and confirm `<title>Mentions Légales | BRICON ANATHOLY</title>` and description matches "Selenium Phase 02". Check other pages have no "freelance" or "hire" in title/description.

5. **QA-04:** Read `VERIFICATION.md` — confirm git diff result is PASS (empty output).

6. Mark each checklist item with [x] or [ ] in `04-QA-CHECKLIST.md` as you go.
  </how-to-verify>

  <resume-signal>
    Type "approved" if all checklist items pass, or describe which items failed and what you observed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search crawlers → metadata | Title and description consumed by Google — must contain no misleading freelance signals |
| git history → diff check | Baseline commit integrity — diff confirms no accidental feuillette changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | SEO metadata (QA-03) | mitigate | grep check + human verify confirms no freelance/hire signals in any page title or description |
| T-04-04-02 | Tampering | /demo/feuillette (QA-04) | mitigate | git diff against pre-Phase-1 baseline; empty output required to pass; documented in VERIFICATION.md |
| T-04-04-03 | Denial of Service | animation at reduced-motion (QA-02) | mitigate | Code check confirms media query handled; human verify confirms no motion where disabled |
</threat_model>

<verification>
After both tasks complete and human has approved:
1. `test -f .planning/phases/04-rethemes-qa/04-QA-CHECKLIST.md` — must pass
2. `test -f .planning/phases/04-rethemes-qa/VERIFICATION.md` — must pass
3. `grep "PASS\|FAIL" .planning/phases/04-rethemes-qa/VERIFICATION.md` — QA-04 result explicitly stated
4. Human checkpoint returned "approved" — all checklist items verified
</verification>

<success_criteria>
- `04-QA-CHECKLIST.md` exists with checklist items for all four requirements (QA-01 through QA-04)
- `VERIFICATION.md` contains QA-04 diff result with baseline commit hash — PASS = empty diff
- QA-03 grep confirms zero freelance/hire/developer keywords in any metadata block
- Human has verified QA-01 (900px no horizontal scroll on all 4 pages), QA-02 (animations respect reduced-motion), QA-03 (metadata correct on all pages)
- Phase 4 complete: every remaining page uses the new design system, i18n coverage audited, integration QA passed
</success_criteria>

<output>
After completion, create `.planning/phases/04-rethemes-qa/04-04-SUMMARY.md`
</output>
