---
phase: 04-rethemes-qa
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - src/context/translations.ts
autonomous: true
requirements:
  - I18N-01
  - I18N-02

must_haves:
  truths:
    - "All landing page components route user-facing strings through useLanguage() — no hardcoded French UI strings"
    - "All services page components route user-facing strings through useLanguage() — no hardcoded French UI strings"
    - "Navbar and Footer use only t.* keys for user-facing strings"
    - "Dead freelance-era keys in translations.ts are flagged in plan summary (not silently passed)"
    - "Any key referenced in components but absent from Translations type is flagged"
    - "i18n coverage report produced in SUMMARY.md"
  artifacts:
    - path: "src/context/translations.ts"
      provides: "Cleaned translation keys — no dead freelance-era keys (if any deletions are safe)"
    - path: ".planning/phases/04-rethemes-qa/04-03-SUMMARY.md"
      provides: "i18n coverage report with hardcoded string list and dead key list"
  key_links:
    - from: "src/components/sections/*.tsx"
      to: "src/context/LanguageContext.tsx"
      via: "useLanguage() hook consuming t.* keys"
      pattern: "useLanguage\\(\\)"
---

<objective>
Audit i18n key coverage across all landing and services section components, Navbar, and Footer. Identify any hardcoded user-facing strings not routed through useLanguage(). Cross-reference against landing-i18n.jsx and services-i18n.jsx key trees. Flag dead freelance-era keys in translations.ts. Produce coverage report in SUMMARY.md.

Purpose: I18N-01 and I18N-02 — all new landing and services content is wired to LanguageContext (fr/en/th).
Output: i18n audit complete; any gaps flagged in SUMMARY.md; dead keys flagged; translations.ts cleaned if safe removals exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rethemes-qa/04-CONTEXT.md

<interfaces>
<!-- LanguageContext hook interface (src/context/LanguageContext.tsx lines 6-10) -->
```tsx
interface LanguageContextType {
  lang: Lang;
  setLang: (lang: Lang) => void;
  t: Translations;
}

// Usage patterns:
const { t, lang, setLang } = useLanguage();  // Navbar (full destructure)
const { t } = useLanguage();                 // Footer, section components (t-only)
```

<!-- Known live translation key namespaces (from translations.ts):
  Phase 2 landing: t.landing.hero, t.landing.manifeste, t.landing.work,
                   t.landing.phone, t.landing.partners, t.landing.contact,
                   t.landing.footer
  Phase 3 services: t.services.hero, t.services.problem, t.services.approach,
                    t.services.offers, t.services.phone, t.services.maintenance,
                    t.services.upsell, t.services.method, t.services.reassurance,
                    t.services.finalCta
  Navbar: t.nav.manifeste, t.nav.work, t.nav.services, t.nav.contact
  Dead legacy (known): t.hero.*, t.about.*, t.projects.*, t.contact.*, t.footer.*,
                       t.phoneAgent.* — these are freelance-era keys
-->

<!-- i18n source files to cross-reference:
  C:/Users/Anatholy/Downloads/portfolio/landing-i18n.jsx
  C:/Users/Anatholy/Downloads/portfolio/services-i18n.jsx
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep audit — find hardcoded strings and t.* usages in all Phase 2/3 components</name>
  <files>src/context/translations.ts</files>

  <read_first>
    - src/context/LanguageContext.tsx — read full file to get Translations type definition and all registered keys
    - src/context/translations.ts — read full file to identify all keys (live Phase 2/3 keys and dead legacy keys)
    - C:/Users/Anatholy/Downloads/portfolio/landing-i18n.jsx — canonical source for landing keys
    - C:/Users/Anatholy/Downloads/portfolio/services-i18n.jsx — canonical source for services keys
    - src/components/layout/Navbar.tsx — confirm t.nav.* keys used
    - src/components/layout/Footer.tsx — confirm t.landing.footer.* keys used
  </read_first>

  <action>
Perform the i18n coverage audit in two passes.

**Pass 1: grep for t.* key usages across all Phase 2/3 components.**

Run these searches mentally (or via Bash grep) and record results:
1. `grep -rn "useLanguage\(\)" src/components/sections/ src/components/layout/ src/app/page.tsx src/app/services/page.tsx` — list all files that call useLanguage()
2. `grep -rn "t\." src/components/sections/ src/components/layout/ src/app/page.tsx src/app/services/page.tsx` — list all t.* key usages
3. For each component that does NOT call useLanguage() but contains user-facing string literals (French text), flag as a hardcoded string gap.

**Pass 2: cross-reference Translations type vs. i18n source files.**

Compare keys in `C:/Users/Anatholy/Downloads/portfolio/landing-i18n.jsx` and `services-i18n.jsx` against the `Translations` interface in `LanguageContext.tsx`. Flag any key present in one source but absent from the other.

**Known dead keys to verify and flag (from PATTERNS.md):**
- `t.hero.*` — freelance hero section (check: not used by any Phase 2/3 component)
- `t.about.*` — freelance about section (check: not used)
- `t.projects.*` — freelance projects section (check: not used)
- `t.contact.*` (legacy) — check if superseded by `t.landing.contact.*`
- `t.footer.*` (legacy) — check if superseded by `t.landing.footer.*`
- `t.phoneAgent.*` (legacy) — check if superseded by `t.landing.phone.*`

**Known flags from PATTERNS.md to document:**
- `t.hero.description` contains "développeur Full Stack & ingénieur IA freelance" inline HTML string
- `t.about.p5_available` contains "Disponible pour des projets freelance"
- `landing-i18n.jsx` has an `intro` key (cinema intro) — verify if `CinemaIntro.tsx` reads from translations or uses its own string
- `services-i18n.jsx` has a `backToHome` key — verify if any services component references `t.services.backToHome`

**If dead keys confirmed unused:** Remove them from `translations.ts` in the same edit. Only remove keys that have zero matches in any component grep scan. If in doubt, keep and flag as "stale, not removed (safety)".

**If any hardcoded string gaps found:** Document in SUMMARY.md with the file path, line number, and the hardcoded string. Do NOT silently pass.

Produce all findings ready for SUMMARY.md i18n coverage report section.
  </action>

  <verify>
    <automated>
      grep -rn "t\.hero\.\|t\.about\.\|t\.projects\.\|t\.phoneAgent\." src/components/ src/app/page.tsx src/app/services/page.tsx src/components/layout/Navbar.tsx src/components/layout/Footer.tsx 2>/dev/null | grep -v "translations.ts"
    </automated>
  </verify>

  <done>
    - Command above returns zero matches (legacy t.hero/t.about/t.projects/t.phoneAgent keys not consumed by any live component)
    - `grep -rn "useLanguage()" src/components/sections/` returns matches (section components use the hook)
    - Audit findings are ready for SUMMARY.md: list of any hardcoded strings found, list of dead keys, list of missing Translations type registrations
    - If dead keys removed from translations.ts: `grep "t\.hero\b\|t\.about\b" src/context/translations.ts` returns zero matches
  </done>
</task>

<task type="auto">
  <name>Task 2: Produce i18n coverage report in SUMMARY.md</name>
  <files>.planning/phases/04-rethemes-qa/04-03-SUMMARY.md</files>

  <read_first>
    - .planning/phases/04-rethemes-qa/04-03-SUMMARY.md — will be created fresh; read if it already exists
    - Findings from Task 1 grep pass
  </read_first>

  <action>
Create `.planning/phases/04-rethemes-qa/04-03-SUMMARY.md` following the standard GSD summary template. In the summary, include a dedicated `## i18n Coverage Report` section with:

**Required sub-sections:**

1. **Hardcoded string gaps** — table of any user-facing strings found NOT routed through useLanguage(). Columns: File | Line | String | Action. If none found, write "None found — all user-facing strings routed through useLanguage()."

2. **Dead keys in translations.ts** — table of legacy freelance-era keys confirmed unused. Columns: Key path | Contains freelance signal | Action taken (removed / kept+flagged). List:
   - `t.hero.*` — status
   - `t.about.*` — status
   - `t.projects.*` — status
   - `t.contact.*` legacy vs `t.landing.contact.*` active — status
   - `t.footer.*` legacy vs `t.landing.footer.*` active — status
   - `t.phoneAgent.*` legacy vs `t.landing.phone.*` active — status

3. **i18n source vs Translations type gaps** — table of keys present in landing-i18n.jsx or services-i18n.jsx but absent from the Translations interface. Columns: Key | Source file | Status. Known suspects: `intro` (landing-i18n), `backToHome` (services-i18n).

4. **Verdict** — one of:
   - "I18N-01 PASS — all landing content keys resolve in fr/en/th"
   - "I18N-02 PASS — all services content keys resolve in fr/en/th"
   - "FLAG: [specific issue] — requires follow-up before v1.0"
  </action>

  <verify>
    <automated>
      test -f .planning/phases/04-rethemes-qa/04-03-SUMMARY.md && grep -c "i18n Coverage Report" .planning/phases/04-rethemes-qa/04-03-SUMMARY.md
    </automated>
  </verify>

  <done>
    - `.planning/phases/04-rethemes-qa/04-03-SUMMARY.md` exists
    - File contains `## i18n Coverage Report` section
    - File contains "Hardcoded string gaps" sub-section
    - File contains "Dead keys" sub-section with verdict per key (removed / flagged)
    - File contains I18N-01 and I18N-02 verdict lines
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translations.ts → component render | Translation values rendered directly as text — no user input, no XSS risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | translations.ts dead keys | mitigate | Remove or flag dead freelance-era keys that contain positioning signals per D-08, D-10 |
| T-04-03-02 | Tampering | translations.ts key removal | accept | Removal only targets keys with zero component grep matches; safety check built into Task 1 action |
</threat_model>

<verification>
After both tasks:
1. `grep -rn "t\.hero\.\|t\.about\.\|t\.projects\.\|t\.phoneAgent\." src/components/ src/app/page.tsx src/app/services/page.tsx` — must return zero matches (legacy keys not consumed)
2. `test -f .planning/phases/04-rethemes-qa/04-03-SUMMARY.md` — must pass
3. `grep "I18N-01\|I18N-02" .planning/phases/04-rethemes-qa/04-03-SUMMARY.md` — must return verdict lines for both
</verification>

<success_criteria>
- All landing section components (src/components/sections/) that render user-facing French strings call useLanguage() — no hardcoded French UI text leaks
- All services section components that render user-facing strings call useLanguage()
- Navbar and Footer: all user-facing link labels and locale strings go through t.*
- Dead freelance-era keys (t.hero, t.about, t.projects) are either removed from translations.ts or explicitly flagged in SUMMARY.md as "stale, kept for safety"
- I18N-01 and I18N-02 verdict written in 04-03-SUMMARY.md — no silent pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-rethemes-qa/04-03-SUMMARY.md` (this plan produces the SUMMARY as Task 2 — the file IS the deliverable for this plan)
</output>
