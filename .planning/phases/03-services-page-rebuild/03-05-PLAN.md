---
wave: 3
plan_id: 03-05
phase: 3
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - src/components/sections/ReassuranceSection.tsx
  - src/components/sections/FinalCtaSection.tsx
  - src/app/services/page.tsx
autonomous: true
requirements: [SVC-09, SVC-10]

must_haves:
  truths:
    - "ReassuranceSection renders 4 trust points with sequential 01-04 numbering"
    - "FinalCtaSection renders 3-line split title, description, 2 CTAs (href=/#contact, mailto), and note"
    - "services/page.tsx imports all 9 section components in correct order"
    - "MethodologySectionLazy imported from ClientProviders and placed in correct order (section 8)"
    - "useReveals() called once in ServicesPage"
    - "PartnersBanner reused directly from Phase 2 between FinalCTA and Footer"
    - "Navbar and Footer imported from layout components — not rebuilt"
    - "page.tsx has 'use client' directive and no metadata export"
  artifacts:
    - path: "src/components/sections/ReassuranceSection.tsx"
      provides: "4 trust points with 01-04 sequential numbering"
      contains: "reassure-grid"
    - path: "src/components/sections/FinalCtaSection.tsx"
      provides: "3-line final CTA with two action buttons"
      contains: "final-cta"
    - path: "src/app/services/page.tsx"
      provides: "Complete services page wiring all 9 sections + layout"
      contains: "MethodologySectionLazy"
  key_links:
    - from: "src/app/services/page.tsx"
      to: "src/components/sections/MethodologySection.tsx"
      via: "MethodologySectionLazy from ClientProviders"
      pattern: "MethodologySectionLazy"
    - from: "src/app/services/page.tsx"
      to: "src/hooks/useReveals.ts"
      via: "useReveals() call"
      pattern: "useReveals()"
    - from: "FinalCtaSection.tsx"
      to: "/#contact"
      via: "primary CTA href"
      pattern: "href=\"/#contact\""
---

<objective>
Build ReassuranceSection and FinalCtaSection, then rewrite services/page.tsx to wire all 9 sections together with proper layout, useReveals, and SEO.

Purpose: This is the final assembly plan — every prior section component converges here.
Output: 2 new section components + rewritten services/page.tsx that renders the complete services page.
</objective>

<execution_context>
@C:/Users/anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-services-page-rebuild/03-CONTEXT.md
@.planning/phases/03-services-page-rebuild/03-02-SUMMARY.md
@.planning/phases/03-services-page-rebuild/03-03-SUMMARY.md
@.planning/phases/03-services-page-rebuild/03-04-SUMMARY.md

<interfaces>
<!-- All imports needed for page.tsx — executor reads these, no exploration needed -->

Section components created in Plans 02-04:
- ServicesHeroSection → `@/components/sections/ServicesHeroSection`
- ProblemSection → `@/components/sections/ProblemSection`
- ApproachSection → `@/components/sections/ApproachSection`
- OffersSection → `@/components/sections/OffersSection`
- PhoneAgentExplainer → `@/components/sections/PhoneAgentExplainer`
- MaintenanceSection → `@/components/sections/MaintenanceSection`
- OptionsSection → `@/components/sections/OptionsSection`
- MethodologySectionLazy (named export) → `@/components/ui/ClientProviders`

Reused Phase 2 components (per D-11 — DO NOT REBUILD):
- Navbar → `@/components/layout/Navbar`
- Footer → `@/components/layout/Footer`
- PartnersBanner → `@/components/sections/PartnersBanner`

Hooks and context:
- useReveals → `@/hooks/useReveals` (call once in ServicesPage, no args needed)
- useLanguage is called INSIDE each section component — NOT in page.tsx

Page rules from codebase decisions (STATE.md):
- page.tsx MUST have 'use client' (Next.js App Router — page imports client components)
- page.tsx must NOT export metadata (that's in layout.tsx — App Router rule)
- useReveals() must be called from the page component, not layout.tsx

Section order (9 sections in order per D-04):
1. ServicesHeroSection
2. ProblemSection
3. ApproachSection
4. OffersSection
5. PhoneAgentExplainer
6. MaintenanceSection
7. OptionsSection
8. MethodologySectionLazy (GSAP — from ClientProviders)
9. ReassuranceSection
   (then PartnersBanner + FinalCtaSection + Footer)

From services-app.jsx SReassure (lines 386-411):
```jsx
<div className="reassure-grid">
  {t.reassurance.points.map((p, i) => (
    <div className="reassure" key={i} data-reveal data-reveal-delay={String(i % 2)}>
      <span className="rn">{String(i + 1).padStart(2, '0')}</span>
      <div>
        <h4>{p.t}</h4>
        <p>{p.d}</p>
      </div>
    </div>
  ))}
</div>
```

From services-app.jsx SFinalCTA (lines 443-463):
```jsx
<section className="final-cta" id="contact-final">
  <div className="wrap">
    <div className="num" data-reveal>{t.finalCta.num}</div>
    <h2 className="final-title split">
      <span className="line"><span>{t.finalCta.title_l1}</span></span>
      <span className="line"><span>{t.finalCta.title_l2}</span></span>
      <span className="line"><span><em className="it">{t.finalCta.title_l3_it}</em></span></span>
    </h2>
    <p className="final-desc" data-reveal data-reveal-delay="1">{t.finalCta.desc}</p>
    <div className="final-buttons" data-reveal data-reveal-delay="2">
      <a href="...#contact" className="btn btn-primary">{t.finalCta.cta} →</a>
      <a href="mailto:business@contact-selenium-studio.com" className="btn btn-ghost">{t.finalCta.email}</a>
    </div>
    <div className="final-note" data-reveal data-reveal-delay="3">{t.finalCta.note}</div>
  </div>
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReassuranceSection + FinalCtaSection</name>
  <files>src/components/sections/ReassuranceSection.tsx, src/components/sections/FinalCtaSection.tsx</files>

  <read_first>
    - C:/Users/Anatholy/Downloads/portfolio/services-app.jsx (SReassure lines 386-411, SFinalCTA lines 443-463)
    - C:/Users/Anatholy/Downloads/portfolio/03 Services.html (reassure-grid, reassure, rn, final-cta, final-title CSS)
  </read_first>

  <action>
Create TWO files:

**File 1: `src/components/sections/ReassuranceSection.tsx`** — export default `ReassuranceSection`
- `'use client'`, useLanguage, `const ts = t.services.reassurance;`
- Section: `<section className="sec border-t">`
- sec-head with `data-reveal`: sec-num (`{ts.num}` = "08 / 09"), title (`{ts.title_l1}<br /><em className="it">{ts.title_l2_it}</em>`), sec-intro (`{ts.intro}`)
- `<div className="reassure-grid">`: map `ts.points` (4 items):
  - `<div className="reassure" key={i} data-reveal data-reveal-delay={String(i % 2)}>`
  - `<span className="rn">{String(i + 1).padStart(2, '0')}</span>` — sequential 01-04 numbering
  - `<div><h4>{p.t}</h4><p>{p.d}</p></div>`

**File 2: `src/components/sections/FinalCtaSection.tsx`** — export default `FinalCtaSection`
- `'use client'`, useLanguage, `const ts = t.services.finalCta;`
- Section: `<section className="final-cta" id="contact-final">`
- `<div className="wrap">`
- Section num: `<div className="num" data-reveal>{ts.num}</div>` — "09 / 09"
- Final title (`final-title split`): 3-line split identical to hero pattern:
  - `<span className="line"><span>{ts.title_l1}</span></span>`
  - `<span className="line"><span>{ts.title_l2}</span></span>`
  - `<span className="line"><span><em className="it">{ts.title_l3_it}</em></span></span>`
- Description: `<p className="final-desc" data-reveal data-reveal-delay="1">{ts.desc}</p>`
- Buttons row (`final-buttons`, `data-reveal data-reveal-delay="2"`):
  - Primary: `<a href="/#contact" className="btn btn-primary">{ts.cta} <span className="ar">→</span></a>` (per D-10: absolute href)
  - Secondary: `<a href="mailto:business@contact-selenium-studio.com" className="btn btn-ghost">{ts.email}</a>`
- Note: `<div className="final-note" data-reveal data-reveal-delay="3">{ts.note}</div>`
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ReassuranceSection|FinalCtaSection"</automated>
  </verify>

  <acceptance_criteria>
    - `src/components/sections/ReassuranceSection.tsx` exists
    - ReassuranceSection contains `t.services.reassurance`
    - ReassuranceSection contains `reassure-grid` and `reassure` classNames
    - ReassuranceSection contains `rn` className with `padStart(2, '0')` — 01-04 numbering
    - `src/components/sections/FinalCtaSection.tsx` exists
    - FinalCtaSection contains `t.services.finalCta`
    - FinalCtaSection contains `id="contact-final"` on the section element
    - FinalCtaSection contains `className="final-cta"` and `final-title split`
    - FinalCtaSection contains `href="/#contact"` (absolute, per D-10)
    - FinalCtaSection contains `href="mailto:business@contact-selenium-studio.com"`
    - Both files contain `'use client'` and `useLanguage`
    - Neither file contains hex color literals
    - `npx tsc --noEmit` passes
  </acceptance_criteria>

  <done>ReassuranceSection.tsx: 4 trust points numbered 01-04. FinalCtaSection.tsx: 3-line title, 2 CTAs, note. Both pass tsc.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite services/page.tsx — wire all 9 sections + layout</name>
  <files>src/app/services/page.tsx</files>

  <read_first>
    - src/app/services/page.tsx (current freelance-era file — read to understand what to replace)
    - src/app/services/layout.tsx (contains metadata + ServicesLayout — do NOT modify this file)
    - src/app/page.tsx (landing page — read how useReveals is called and how components are assembled)
    - src/components/ui/ClientProviders.tsx (verify MethodologySectionLazy export is present after Plan 04)
  </read_first>

  <action>
Completely rewrite `src/app/services/page.tsx`. This is a full file replacement.

The new file must:

1. Start with `'use client';` directive
2. Import all section components in order:
   ```tsx
   import Navbar from '@/components/layout/Navbar';
   import Footer from '@/components/layout/Footer';
   import PartnersBanner from '@/components/sections/PartnersBanner';
   import ServicesHeroSection from '@/components/sections/ServicesHeroSection';
   import ProblemSection from '@/components/sections/ProblemSection';
   import ApproachSection from '@/components/sections/ApproachSection';
   import OffersSection from '@/components/sections/OffersSection';
   import PhoneAgentExplainer from '@/components/sections/PhoneAgentExplainer';
   import MaintenanceSection from '@/components/sections/MaintenanceSection';
   import OptionsSection from '@/components/sections/OptionsSection';
   import { MethodologySectionLazy } from '@/components/ui/ClientProviders';
   import ReassuranceSection from '@/components/sections/ReassuranceSection';
   import FinalCtaSection from '@/components/sections/FinalCtaSection';
   import { useReveals } from '@/hooks/useReveals';
   ```
3. Define `export default function ServicesPage()`:
   - Call `useReveals();` at the top of the function body (activates data-reveal globally)
   - Return JSX with sections in EXACT order per D-04:
   ```tsx
   <>
     <Navbar />
     <main>
       <ServicesHeroSection />
       <ProblemSection />
       <ApproachSection />
       <OffersSection />
       <PhoneAgentExplainer />
       <MaintenanceSection />
       <OptionsSection />
       <MethodologySectionLazy />
       <ReassuranceSection />
       <PartnersBanner />
       <FinalCtaSection />
     </main>
     <Footer />
   </>
   ```

4. Do NOT export metadata — that lives in src/app/services/layout.tsx (per STATE.md decision)
5. Do NOT redeclare Navbar, Footer, or any sections inline — import only

Per D-11: Navbar, Footer, PartnersBanner are Phase 2 components — reuse directly, no changes.
Per D-06: MethodologySectionLazy is already the ssr:false wrapper from ClientProviders.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/services/page.tsx` starts with `'use client';`
    - File contains `import { MethodologySectionLazy } from '@/components/ui/ClientProviders'`
    - File contains `useReveals()` call inside ServicesPage function
    - File renders sections in order: ServicesHeroSection, ProblemSection, ApproachSection, OffersSection, PhoneAgentExplainer, MaintenanceSection, OptionsSection, MethodologySectionLazy, ReassuranceSection, PartnersBanner, FinalCtaSection
    - File does NOT contain `export const metadata` (belongs in layout.tsx)
    - File does NOT contain inline section component definitions (no `function ServicesHero()` etc.)
    - File does NOT contain hex color literals
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>

  <done>services/page.tsx: clean rewrite with all 9 sections in order, MethodologySectionLazy, useReveals, Navbar/Footer — no type errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx → section components | Static rendering — no user data at assembly layer |
| FinalCtaSection mailto link | Email address in public HTML — intentional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | mailto in FinalCtaSection | accept | Agency contact email is intentionally public on marketing page |
</threat_model>

<verification>
- `npx tsc --noEmit` passes — zero errors across the entire project
- `grep "useReveals" src/app/services/page.tsx` returns the hook call
- `grep "MethodologySectionLazy" src/app/services/page.tsx` returns the import and usage
- `grep "export const metadata" src/app/services/page.tsx` returns EMPTY (no metadata in page)
- `grep "metadata" src/app/services/layout.tsx` returns the SEO metadata (unchanged)
- `ls src/components/sections/` shows all new section files present
</verification>

<success_criteria>
- ReassuranceSection.tsx: 4 trust points with rn class 01-04 numbering, data-reveal stagger
- FinalCtaSection.tsx: final-cta section, 3-line split title, absolute href="/#contact" + mailto CTA, note
- services/page.tsx: 'use client', useReveals, 9 sections in order + PartnersBanner + Footer, no inline definitions, no metadata export
- Full project: `npx tsc --noEmit` clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-services-page-rebuild/03-05-SUMMARY.md`
</output>
