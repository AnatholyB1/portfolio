---
wave: 4
plan_id: 03-06
phase: 3
depends_on: [03-05]
files_modified:
  - src/app/globals.css
autonomous: false
requirements: [SVC-01, SVC-02, SVC-03, SVC-04, SVC-05, SVC-06, SVC-07, SVC-08, SVC-09, SVC-10]

must_haves:
  truths:
    - "Services page renders visually at localhost:3000/services with correct layout"
    - "Hero 3-line title visible with display font (Bricolage Grotesque)"
    - "Problem cards show warm accent (var(--warm)) on the pn letter labels"
    - "Methodology rail line and active-fill animate as user scrolls through 6 steps"
    - "Active methodology step shows acid color (var(--acid)) on num-big and bold on h3"
    - "All 9 section titles have consistent sec-num styling (monospace, ink-dim)"
    - "No hardcoded hex in globals.css new services rules"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Services-specific CSS appended after existing Phase 2 rules"
      contains: ".s-hero"
  key_links:
    - from: "globals.css .pn"
      to: "var(--warm)"
      via: "color property"
      pattern: "\\.pn"
    - from: "globals.css .method-step.active .num-big"
      to: "var(--acid)"
      via: "color property"
      pattern: "method-step\\.active"
---

<objective>
Add services-specific CSS to globals.css and run a human checkpoint to visually verify the complete services page.

Purpose: Per D-03 (locked), Phase 3 ends with a dedicated CSS plan + human verification checkpoint, mirroring Phase 2's 02-06 structure.
Output: Services CSS appended to globals.css + human sign-off that the page looks correct.
</objective>

<execution_context>
@C:/Users/anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-services-page-rebuild/03-CONTEXT.md
@.planning/phases/03-services-page-rebuild/03-05-SUMMARY.md

<interfaces>
<!-- CSS vars available in globals.css (from Phase 1) -->
:root {
  --bg:        #0A0B0C;
  --bg-2:      #111213;
  --acid:      #C4F542;
  --ink:       #ECEAE3;
  --ink-dim:   rgba(236,234,227,0.55);
  --ink-faint: rgba(236,234,227,0.25);
  --warm:      #E07856;
  --line:      #1F1F1F;
}

CSS class names used in section components (from Plans 02-05):
Hero: .s-hero, .s-hero-grid, .s-hero-top, .s-hero-title, .s-hero-bottom, .s-hero-meta, .s-hero-sub, .s-hero-ctas, .crumb-back, .s-pill, .dot, .ar
Section shells: .sec, .sec.border-t, .wrap, .sec-head, .sec-num, .sec-title, .sec-intro, .it (italic)
Problem: .problem-grid, .problem-card, .pn, .problem-good
Approach: .approach, .approach-text, .approach-cards, .approach-card, .benefits, .benefit, .idx
Offers: .offers-grid, .offer, .offer.popular, .pop-tag, .o-name, .o-tag, .o-from, .o-price, .o-desc, .o-feats, .o-feat, .o-cta
Phone: .phone-steps, .ph-step, .ico, .phone-cta
Maintenance: .maintenance-grid, .mpack, .mpack.popular, .price-row, .pcur, .pval, .pnote, .pper, .mdesc, .mfeats, .mfeat, .perks, .perk
Upsell: .upsell-grid, .upsell-card, .un
Method: .method, .method-rail, .method-rail-active, .method-steps, .method-step, .method-step.active, .ndot, .meta, .num-big
Reassurance: .reassure-grid, .reassure, .rn
Final CTA: .final-cta, .final-title, .final-desc, .final-buttons, .final-note, .num
Shared: .btn, .btn-primary, .btn-ghost, .c (checkmark/arrow prefix)

Phase 2 CSS pattern to follow (check globals.css for how Phase 2 added its rules):
- CSS appended in a `/* ─── Services Page ─── */` block after Phase 2 rules
- Use CSS vars exclusively — var(--acid), var(--warm), var(--ink), etc.
- Responsive at 900px min breakpoint via @media (min-width: 900px)
- Mobile-first: base styles for mobile, enhanced at ≥900px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write services CSS block in globals.css</name>
  <files>src/app/globals.css</files>

  <read_first>
    - src/app/globals.css (FULL FILE — read all existing rules to understand the end of Phase 2 CSS where new rules must be appended, and to verify no duplicate class definitions)
    - C:/Users/Anatholy/Downloads/portfolio/03 Services.html (full CSS section — extract the visual patterns for each class listed above)
    - src/components/sections/ServicesHeroSection.tsx (verify class names used)
    - src/components/sections/MethodologySection.tsx (verify method-rail, ndot, num-big classes)
  </read_first>

  <action>
Append a `/* ─── Services Page ─── */` CSS block to the END of `src/app/globals.css`, after all existing Phase 2 rules. Do not modify any existing rules.

The block must define styles for all CSS classes used by the 9 section components. Reference `03 Services.html` for the visual design. All colors must use CSS vars — never hex.

Key rules to include (derive exact values from 03 Services.html):

**Section shell:**
```css
.sec { padding: 5rem 0; }
.sec.border-t { border-top: 1px solid var(--line); }
.wrap { max-width: 1200px; margin: 0 auto; padding: 0 1.5rem; }
.sec-head { margin-bottom: 4rem; }
.sec-num { font-family: var(--font-mono); font-size: 0.75rem; color: var(--ink-dim); letter-spacing: 0.12em; margin-bottom: 0.75rem; }
.sec-title { font-family: var(--font-display); font-size: clamp(2.5rem, 5vw, 4rem); color: var(--ink); line-height: 1; margin-bottom: 1.5rem; }
.sec-title .it { font-style: italic; color: var(--acid); }
.sec-intro { font-size: 1.1rem; color: var(--ink-dim); max-width: 640px; line-height: 1.7; }
```

**Hero:**
```css
.s-hero { min-height: 100svh; display: flex; flex-direction: column; padding: 0; background: var(--bg); position: relative; overflow: hidden; }
.s-hero-grid { position: absolute; inset: 0; background-image: linear-gradient(var(--line) 1px, transparent 1px), linear-gradient(90deg, var(--line) 1px, transparent 1px); background-size: 60px 60px; opacity: 0.4; pointer-events: none; }
.s-hero-top { display: flex; align-items: center; justify-content: space-between; padding: 1.5rem; }
.crumb-back { font-family: var(--font-mono); font-size: 0.75rem; color: var(--ink-dim); text-decoration: none; letter-spacing: 0.1em; }
.crumb-back:hover { color: var(--ink); }
.s-pill { display: flex; align-items: center; gap: 0.5rem; font-family: var(--font-mono); font-size: 0.7rem; color: var(--ink-dim); letter-spacing: 0.1em; }
.s-pill .dot { width: 6px; height: 6px; border-radius: 50%; background: var(--acid); }
.s-hero-title { font-family: var(--font-display); font-size: clamp(4rem, 12vw, 10rem); line-height: 0.95; color: var(--ink); padding: 2rem 1.5rem; }
.s-hero-title .it { font-style: italic; color: var(--acid); }
.s-hero-bottom { display: flex; flex-direction: column; gap: 2rem; padding: 2rem 1.5rem; border-top: 1px solid var(--line); }
.s-hero-sub { font-size: 1rem; color: var(--ink-dim); max-width: 480px; line-height: 1.7; margin-bottom: 1.5rem; }
.s-hero-ctas { display: flex; flex-wrap: wrap; gap: 1rem; }
.s-hero-meta { display: flex; flex-direction: column; gap: 0.25rem; }
.s-hero-meta .k { font-family: var(--font-mono); font-size: 0.7rem; color: var(--ink-faint); letter-spacing: 0.1em; text-transform: uppercase; }
.s-hero-meta .v { font-family: var(--font-display); font-size: 1.75rem; color: var(--ink); line-height: 1; }
@media (min-width: 900px) {
  .s-hero-bottom { flex-direction: row; align-items: flex-end; justify-content: space-between; }
  .s-hero-title { font-size: clamp(6rem, 14vw, 12rem); }
}
```

**Shared buttons:**
```css
.btn { display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.875rem 2rem; font-family: var(--font-body); font-size: 0.9rem; font-weight: 600; border-radius: 0; text-decoration: none; transition: all 0.2s; }
.btn-primary { background: var(--acid); color: var(--bg); }
.btn-primary:hover { background: var(--ink); }
.btn-ghost { border: 1px solid var(--line); color: var(--ink); }
.btn-ghost:hover { border-color: var(--ink-dim); }
.ar { font-family: var(--font-mono); }
.c { color: var(--acid); font-family: var(--font-mono); }
```

**Problem:**
```css
.problem-grid { display: grid; grid-template-columns: 1fr; gap: 1px; background: var(--line); border: 1px solid var(--line); }
.problem-card { background: var(--bg); padding: 2rem; }
.pn { display: block; font-family: var(--font-mono); font-size: 2rem; color: var(--warm); margin-bottom: 1rem; line-height: 1; }
.problem-card h3 { font-family: var(--font-display); font-size: 1.3rem; color: var(--ink); margin-bottom: 0.75rem; }
.problem-card p { color: var(--ink-dim); line-height: 1.7; }
.problem-good { margin-top: 3rem; padding: 1.5rem 2rem; border-left: 3px solid var(--acid); color: var(--ink); font-size: 1rem; }
@media (min-width: 900px) {
  .problem-grid { grid-template-columns: 1fr 1fr; }
}
```

**Approach:**
```css
.approach { display: grid; gap: 3rem; }
.benefits { display: flex; flex-direction: column; gap: 0.75rem; }
.benefit { display: flex; gap: 1rem; align-items: baseline; color: var(--ink-dim); font-size: 1rem; line-height: 1.6; }
.benefit .c { color: var(--acid); flex-shrink: 0; }
.approach-cards { display: flex; flex-direction: column; gap: 1px; background: var(--line); border: 1px solid var(--line); }
.approach-card { background: var(--bg); padding: 1.5rem 2rem; display: flex; gap: 1.5rem; align-items: flex-start; }
.approach-card .idx { font-family: var(--font-mono); font-size: 1.5rem; color: var(--acid); opacity: 0.4; flex-shrink: 0; line-height: 1; }
.approach-card h4 { font-family: var(--font-display); font-size: 1.1rem; color: var(--ink); margin-bottom: 0.25rem; }
.approach-card p { color: var(--ink-dim); font-size: 0.9rem; line-height: 1.6; }
@media (min-width: 900px) {
  .approach { grid-template-columns: 1fr 1fr; align-items: start; }
}
```

**Offers:**
```css
.offers-grid { display: grid; gap: 1px; background: var(--line); border: 1px solid var(--line); }
.offer { background: var(--bg); padding: 2.5rem; position: relative; }
.offer.popular { background: var(--bg-2); }
.pop-tag { position: absolute; top: 1.5rem; right: 1.5rem; font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.12em; color: var(--acid); border: 1px solid var(--acid); padding: 0.25rem 0.5rem; }
.o-name { font-family: var(--font-display); font-size: 1.5rem; color: var(--ink); margin-bottom: 0.25rem; }
.o-tag { font-family: var(--font-mono); font-size: 0.7rem; color: var(--ink-faint); letter-spacing: 0.1em; margin-bottom: 1rem; }
.o-from { font-family: var(--font-mono); font-size: 0.65rem; color: var(--ink-dim); letter-spacing: 0.1em; }
.o-price { font-family: var(--font-display); font-size: 1.75rem; color: var(--ink); margin-bottom: 0.75rem; line-height: 1; }
.o-desc { color: var(--ink-dim); font-size: 0.9rem; line-height: 1.6; margin-bottom: 1.5rem; }
.o-feats { list-style: none; margin-bottom: 2rem; display: flex; flex-direction: column; gap: 0.5rem; }
.o-feat { display: flex; gap: 0.75rem; align-items: baseline; font-size: 0.875rem; color: var(--ink-dim); }
.o-cta { display: inline-block; padding: 0.75rem 1.5rem; background: var(--acid); color: var(--bg); font-family: var(--font-mono); font-size: 0.8rem; letter-spacing: 0.05em; text-decoration: none; transition: background 0.2s; }
.o-cta:hover { background: var(--ink); }
.offer.popular .o-cta { background: var(--acid); }
@media (min-width: 900px) {
  .offers-grid { grid-template-columns: repeat(4, 1fr); }
}
```

**Phone (PhoneAgentExplainer):**
```css
.phone-steps { display: grid; gap: 1.5rem; }
.ph-step { padding: 2rem; border: 1px solid var(--line); }
.ph-step .ico { font-family: var(--font-mono); font-size: 2.5rem; color: var(--acid); margin-bottom: 1rem; line-height: 1; }
.ph-step h4 { font-family: var(--font-display); font-size: 1.1rem; color: var(--ink); margin-bottom: 0.5rem; }
.ph-step p { color: var(--ink-dim); font-size: 0.9rem; line-height: 1.6; }
.phone-cta { margin-top: 2.5rem; }
@media (min-width: 900px) {
  .phone-steps { grid-template-columns: repeat(4, 1fr); }
}
```

**Maintenance:**
```css
.maintenance-grid { display: grid; gap: 1px; background: var(--line); border: 1px solid var(--line); }
.mpack { background: var(--bg); padding: 2.5rem; position: relative; }
.mpack.popular { background: var(--bg-2); }
.price-row { display: flex; align-items: baseline; gap: 0.25rem; margin-bottom: 0.75rem; }
.pcur { font-family: var(--font-mono); font-size: 1rem; color: var(--ink-dim); }
.pval { font-family: var(--font-display); font-size: 3rem; color: var(--ink); line-height: 1; }
.pnote { font-family: var(--font-mono); font-size: 0.8rem; color: var(--ink-dim); }
.pper { font-family: var(--font-mono); font-size: 0.75rem; color: var(--ink-dim); }
.mdesc { color: var(--ink-dim); font-size: 0.875rem; margin-bottom: 1.5rem; line-height: 1.6; }
.mfeats { list-style: none; display: flex; flex-direction: column; gap: 0.5rem; }
.mfeat { display: flex; gap: 0.75rem; align-items: baseline; font-size: 0.875rem; color: var(--ink-dim); }
.perks { display: flex; flex-wrap: wrap; gap: 1rem; margin-top: 2rem; padding-top: 2rem; border-top: 1px solid var(--line); }
.perk { font-family: var(--font-mono); font-size: 0.75rem; color: var(--ink-dim); letter-spacing: 0.08em; }
@media (min-width: 900px) {
  .maintenance-grid { grid-template-columns: repeat(3, 1fr); }
}
```

**Upsell (Options):**
```css
.upsell-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1px; background: var(--line); border: 1px solid var(--line); }
.upsell-card { background: var(--bg); padding: 1.75rem; }
.upsell-card:hover { background: var(--bg-2); }
.un { font-family: var(--font-mono); font-size: 1.25rem; color: var(--acid); opacity: 0.4; margin-bottom: 0.75rem; line-height: 1; }
.upsell-card h4 { font-family: var(--font-display); font-size: 1rem; color: var(--ink); margin-bottom: 0.25rem; }
.upsell-card p { color: var(--ink-dim); font-size: 0.85rem; line-height: 1.5; }
@media (min-width: 900px) {
  .upsell-grid { grid-template-columns: repeat(4, 1fr); }
}
```

**Methodology rail:**
```css
.method { position: relative; padding-left: 2px; }
.method-rail { position: absolute; left: 0; top: 0; bottom: 0; width: 2px; background: var(--line); }
.method-rail-active { position: absolute; left: 0; top: 0; width: 2px; background: var(--acid); transition: height 0.1s linear; }
.method-steps { display: flex; flex-direction: column; gap: 0; }
.method-step { padding: 2.5rem 0 2.5rem 2.5rem; border-bottom: 1px solid var(--line); position: relative; }
.ndot { position: absolute; left: -5px; top: 50%; transform: translateY(-50%); width: 12px; height: 12px; border-radius: 50%; background: var(--line); border: 2px solid var(--line); transition: background 0.3s, border-color 0.3s; }
.method-step.active .ndot { background: var(--acid); border-color: var(--acid); }
.meta { }
.num-big { display: block; font-family: var(--font-mono); font-size: 2.5rem; color: var(--ink-faint); line-height: 1; margin-bottom: 0.5rem; transition: color 0.3s; }
.method-step.active .num-big { color: var(--acid); }
.method-step h3 { font-family: var(--font-display); font-size: 1.5rem; color: var(--ink-dim); margin-bottom: 0.5rem; font-weight: 400; transition: color 0.3s, font-weight 0.3s; }
.method-step.active h3 { color: var(--ink); font-weight: 700; }
.method-step p { color: var(--ink-faint); font-size: 0.9rem; line-height: 1.7; transition: color 0.3s; }
.method-step.active p { color: var(--ink-dim); }
@media (min-width: 900px) {
  .method { padding-left: 3rem; }
  .num-big { font-size: 4rem; }
}
```

**Reassurance:**
```css
.reassure-grid { display: grid; gap: 1px; background: var(--line); border: 1px solid var(--line); }
.reassure { background: var(--bg); padding: 2rem; display: flex; gap: 1.5rem; align-items: flex-start; }
.rn { font-family: var(--font-mono); font-size: 1.5rem; color: var(--acid); opacity: 0.4; flex-shrink: 0; line-height: 1; min-width: 3rem; }
.reassure h4 { font-family: var(--font-display); font-size: 1.2rem; color: var(--ink); margin-bottom: 0.5rem; }
.reassure p { color: var(--ink-dim); font-size: 0.9rem; line-height: 1.7; }
@media (min-width: 900px) {
  .reassure-grid { grid-template-columns: 1fr 1fr; }
}
```

**Final CTA:**
```css
.final-cta { padding: 8rem 0; border-top: 1px solid var(--line); }
.final-cta .num { font-family: var(--font-mono); font-size: 0.75rem; color: var(--ink-faint); letter-spacing: 0.1em; margin-bottom: 2rem; }
.final-title { font-family: var(--font-display); font-size: clamp(3rem, 8vw, 7rem); line-height: 0.95; color: var(--ink); margin-bottom: 2.5rem; }
.final-title .it { font-style: italic; color: var(--acid); }
.final-desc { font-size: 1.1rem; color: var(--ink-dim); max-width: 520px; line-height: 1.7; margin-bottom: 2.5rem; }
.final-buttons { display: flex; flex-wrap: wrap; gap: 1rem; margin-bottom: 2rem; }
.final-note { font-family: var(--font-mono); font-size: 0.7rem; color: var(--ink-faint); letter-spacing: 0.1em; }
```

**Split title animation (reused from Phase 2):**
Verify `.split .line span` reveal animation already exists in globals.css from Phase 1/2. If not, add:
```css
.split .line { overflow: hidden; }
.split .line span { display: inline-block; transform: translateY(110%); transition: transform 0.8s cubic-bezier(0.16,1,0.3,1); }
.split.in .line span { transform: translateY(0); }
.split .line:nth-child(2) span { transition-delay: 0.1s; }
.split .line:nth-child(3) span { transition-delay: 0.2s; }
```

Append everything in one clearly labeled block at the end of the file.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/globals.css` contains `/* ─── Services Page ─── */` comment block
    - File contains `.s-hero` rule
    - File contains `.pn` rule with `color: var(--warm)` (NOT a hex value)
    - File contains `.method-rail` and `.method-rail-active` rules
    - File contains `.method-step.active .num-big` with `color: var(--acid)`
    - File contains `.pop-tag` rule
    - File contains `@media (min-width: 900px)` breakpoints for offers, maintenance, phone-steps grids
    - File does NOT contain hex color literals in the new services block (#XXXXXX patterns)
    - `npx tsc --noEmit` still passes (CSS changes don't affect TypeScript)
  </acceptance_criteria>

  <done>Services CSS block appended to globals.css — all section classes defined with CSS vars, responsive breakpoints at 900px</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete services page rebuild: 9 section components, GSAP methodology rail, i18n in fr/en/th, services CSS, all wired in services/page.tsx.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (or verify it's already running)
    2. Visit http://localhost:3000/services in the browser
    3. Verify these in order:
       a. HERO: "Sites," / "outils," / "agents." 3-line display title visible. Pill badge "OFFRES & TARIFS — 2026". 3 meta stats. Two CTA buttons.
       b. PROBLEM (01/09): 4 dark cards with letters A, B, C, D in warm orange color (var(--warm)). Good news bar below.
       c. APPROACH (02/09): 6 benefit rows with → prefix. 3 numbered cards (01, 02, 03).
       d. OFFERS (03/09): 4 pricing pack cards. Second card (Rebranding) has "Le plus demandé" badge in acid color.
       e. PHONE AGENT (04/09): 4 steps with large monospace numbers 01-04. No scroll animation (static grid).
       f. MAINTENANCE (05/09): 3 packs. Business pack has "Recommandé" badge. Price row with €/month.
       g. OPTIONS (06/09): 8 add-on cards numbered 01-08 in a grid.
       h. METHODOLOGY (07/09): Vertical rail with left line. Scroll through — active step highlights in acid color, num-big turns acid-colored, title turns bold.
       i. REASSURANCE (08/09): 4 trust point cards numbered 01-04.
       j. FINAL CTA (09/09): 3-line display title, 2 buttons.
       k. Footer and Partners ticker appear at the bottom.
       l. Language switcher: click EN and TH — all sections update to English/Thai strings.
       m. Scroll reveals: section titles and cards fade in as you scroll down.
       n. Visit http://localhost:3000 — confirm landing page is visually unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if the page looks correct, or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| globals.css → browser | CSS delivered to client — no secrets, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | globals.css append | accept | CSS file is static build artifact — no runtime mutation possible |
</threat_model>

<verification>
- `npm run build` completes without errors (full build verification)
- `grep "var(--warm)" src/app/globals.css` returns the .pn color rule
- `grep "var(--acid)" src/app/globals.css` returns method-step.active, num-big, acid accent rules
- `grep "#[0-9a-fA-F]\{3,6\}" src/app/globals.css` returns ONLY Phase 1 :root variable definitions, not scattered hex in services rules
- Human checkpoint approved
</verification>

<success_criteria>
- globals.css: services CSS block appended, all section classes styled, var(--warm) on .pn, var(--acid) on active methodology step, responsive grid breakpoints at 900px
- Visual page renders correctly at localhost:3000/services
- All 9 sections visible and correctly ordered
- Methodology rail animates on scroll with active step highlighting
- Language switcher updates all strings in fr/en/th
- Landing page at localhost:3000 unchanged
- Human checkpoint: "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/03-services-page-rebuild/03-06-SUMMARY.md`
</output>
