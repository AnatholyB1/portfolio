---
wave: 1
plan_id: 03-01
phase: 3
depends_on: []
files_modified:
  - src/lib/translations.ts
autonomous: true
requirements: [SVC-01, SVC-02, SVC-03, SVC-04, SVC-05, SVC-06, SVC-07, SVC-08, SVC-09, SVC-10]

must_haves:
  truths:
    - "t.services.hero has title_l1/title_l2/title_l3_it, badge, sub, cta_audit, cta_offers, and meta array (3 items) in fr/en/th"
    - "t.services.problem has num, title_l1, title_l2_it, intro, items (4 with n/title/desc), good_news in fr/en/th"
    - "t.services.approach has num, benefits (6), cards (3 with t/d) in fr/en/th"
    - "t.services.offers has num, items (4 packs with features), from, popular, cta in fr/en/th"
    - "t.services.phone has num, badge, title_l1/l2/l3_it, sub, steps (4 with label/desc), cta in fr/en/th"
    - "t.services.maintenance has num, packs (3: Essentiel/Business/Popular/Premium), perks, currency, per_month in fr/en/th"
    - "t.services.upsell has num, options (8 with name/desc) in fr/en/th"
    - "t.services.method has num, steps (6 with n/t/d) in fr/en/th"
    - "t.services.reassurance has num, points (4 with t/d) in fr/en/th"
    - "t.services.finalCta has num, title_l1/l2/l3_it, desc, cta, email, note in fr/en/th"
  artifacts:
    - path: "src/lib/translations.ts"
      provides: "Complete services namespace for all 3 locales matching services-i18n.jsx"
      contains: "title_l1: 'Sites,'"
  key_links:
    - from: "src/lib/translations.ts"
      to: "src/components/sections/*Section.tsx"
      via: "useLanguage() → t.services.*"
      pattern: "t\\.services\\."
---

<objective>
Replace the freelance-era `services` namespace in translations.ts with the complete Selenium Phase 02 services content for fr/en/th.

Purpose: All section components built in Wave 2 depend on these keys existing. This plan must run first.
Output: Updated translations.ts with new Translations interface and fr/en/th data for the services namespace.
</objective>

<execution_context>
@C:/Users/anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-services-page-rebuild/03-CONTEXT.md

<interfaces>
<!-- Key structural contracts the executor must match exactly -->

From services-i18n.jsx — new services namespace shape (fr locale shown as reference):
```
services: {
  hero: { badge, title_l1, title_l2, title_l3_it, sub, cta_audit, cta_offers, meta: [{k,v}×3] }
  problem: { num, title_l1, title_l2_it, intro, items: [{n,title,desc}×4], good_news }
  approach: { num, title_l1, title_l2_it, intro, benefits: string[6], cards: [{t,d}×3] }
  offers: { num, title_l1, title_l2_it, intro, from, popular, cta, items: [{name,tagline,price,description,features[]}×4] }
  phone: { num, badge, title_l1, title_l2, title_l3_it, sub, steps: [{label,desc}×4], cta }
  maintenance: { num, title_l1, title_l2_it, intro, recommended, per_month, currency, packs: [{name,price,description,features[],popular?,priceNote?}×3], perks: string[3] }
  upsell: { num, title_l1, title_l2_it, intro, options: [{name,desc}×8] }
  method: { num, title_l1, title_l2_it, intro, steps: [{n,t,d}×6] }
  reassurance: { num, title_l1, title_l2_it, intro, points: [{t,d}×4] }
  finalCta: { num, title_l1, title_l2, title_l3_it, desc, cta, email, note }
}
```

From src/lib/translations.ts — current interface (to be replaced in Translations type):
Lines 91-188: old `services` block with `hero.title`, `solution`, `upsell.options[].icon`, etc.
These keys are INCOMPATIBLE with the new structure. Replace entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace services namespace in Translations interface</name>
  <files>src/lib/translations.ts</files>

  <read_first>
    - src/lib/translations.ts (full file — understand current interface shape at lines 91-188 and existing services data for fr/en/th starting ~line 366)
    - C:/Users/Anatholy/Downloads/portfolio/services-i18n.jsx (canonical i18n source — use ALL keys verbatim)
  </read_first>

  <action>
Replace the `services` block inside the `Translations` interface (lines 91-188) with the new shape derived from services-i18n.jsx. The new TypeScript interface must be:

```typescript
services: {
  hero: {
    badge: string;
    title_l1: string;
    title_l2: string;
    title_l3_it: string;
    sub: string;
    cta_audit: string;
    cta_offers: string;
    meta: { k: string; v: string }[];
  };
  problem: {
    num: string;
    title_l1: string;
    title_l2_it: string;
    intro: string;
    items: { n: string; title: string; desc: string }[];
    good_news: string;
  };
  approach: {
    num: string;
    title_l1: string;
    title_l2_it: string;
    intro: string;
    benefits: string[];
    cards: { t: string; d: string }[];
  };
  offers: {
    num: string;
    title_l1: string;
    title_l2_it: string;
    intro: string;
    from: string;
    popular: string;
    cta: string;
    items: { name: string; tagline: string; price: string; description: string; features: string[] }[];
  };
  phone: {
    num: string;
    badge: string;
    title_l1: string;
    title_l2: string;
    title_l3_it: string;
    sub: string;
    steps: { label: string; desc: string }[];
    cta: string;
  };
  maintenance: {
    num: string;
    title_l1: string;
    title_l2_it: string;
    intro: string;
    recommended: string;
    per_month: string;
    currency: string;
    packs: { name: string; price: string; description: string; features: string[]; popular?: boolean; priceNote?: string }[];
    perks: string[];
  };
  upsell: {
    num: string;
    title_l1: string;
    title_l2_it: string;
    intro: string;
    options: { name: string; desc: string }[];
  };
  method: {
    num: string;
    title_l1: string;
    title_l2_it: string;
    intro: string;
    steps: { n: string; t: string; d: string }[];
  };
  reassurance: {
    num: string;
    title_l1: string;
    title_l2_it: string;
    intro: string;
    points: { t: string; d: string }[];
  };
  finalCta: {
    num: string;
    title_l1: string;
    title_l2: string;
    title_l3_it: string;
    desc: string;
    cta: string;
    email: string;
    note: string;
  };
};
```

NOTE: Remove the old `solution`, `partners`, `cta_final`, `phoneAgent`, `methodology`, `upsell.options[].icon` fields from the interface entirely. The `services` block is a complete replacement.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <done>TypeScript interface updated — no tsc errors related to services namespace shape</done>
</task>

<task type="auto">
  <name>Task 2: Replace services data for fr locale</name>
  <files>src/lib/translations.ts</files>

  <read_first>
    - src/lib/translations.ts (find the `fr` locale object, locate existing `services:` block ~line 366)
    - C:/Users/Anatholy/Downloads/portfolio/services-i18n.jsx (fr section — copy ALL content verbatim)
  </read_first>

  <action>
Find the `services:` block inside the `fr` locale object in translations.ts and replace it entirely with the data from services-i18n.jsx `fr` key. Copy all strings verbatim — do not translate, rephrase, or omit. Key mapping:

- `fr.hero` → hero (all fields including meta array of 3 items)
- `fr.problem` → problem (items array: n, title, desc — note: services-i18n uses `desc` not `description`)
- `fr.approach` → approach (benefits[6], cards[3] with t/d)
- `fr.offers` → offers (items[4] with name/tagline/price/description/features[])
- `fr.phone` → phone (steps[4] with label/desc — NOT icon)
- `fr.maintenance` → maintenance (packs[3] with popular on Business, perks[3])
- `fr.upsell` → upsell (options[8] with name/desc — no icon field)
- `fr.method` → method (steps[6] with n/t/d)
- `fr.reassurance` → reassurance (points[4] with t/d — no icon)
- `fr.finalCta` → finalCta (title_l1/l2/l3_it, desc, cta, email, note)

Important per D-04 (strict mockup fidelity): copy strings exactly as they appear in services-i18n.jsx. Do not modify punctuation or capitalization.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <done>fr locale services block matches services-i18n.jsx fr section — tsc passes</done>
</task>

<task type="auto">
  <name>Task 3: Replace services data for en and th locales</name>
  <files>src/lib/translations.ts</files>

  <read_first>
    - src/lib/translations.ts (find en and th locale services blocks)
    - C:/Users/Anatholy/Downloads/portfolio/services-i18n.jsx (en and th sections)
  </read_first>

  <action>
Same process as Task 2 but for `en` (~line 816) and `th` (~line 1266) locale objects. Copy ALL services data verbatim from services-i18n.jsx en and th keys respectively.

After all three locales are updated, run `npx tsc --noEmit` to confirm zero type errors. If any mismatches exist (field name typos, missing array items), fix them before marking done.

Per D-07: these are the source-of-truth strings for all section components. No ad-hoc string additions.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <done>All three locales (fr/en/th) have complete services namespace matching services-i18n.jsx — zero tsc errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translations.ts → components | Static strings, no user input — low risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | translations.ts | accept | Static data file, no runtime mutations — read-only at runtime |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep -n "title_l1" src/lib/translations.ts` returns entries for fr, en, th locales
- `grep -n "Sites," src/lib/translations.ts` returns the fr hero title_l1 string
- `grep -c "method" src/lib/translations.ts` shows method key present in all 3 locales
</verification>

<success_criteria>
- translations.ts Translations interface has new services shape with 10 subsections
- fr, en, th locale data blocks all match services-i18n.jsx content exactly
- `npx tsc --noEmit` passes — no type errors
- Old freelance-era keys (solution, partners in services, cta_final, methodology.steps[].number) are gone
</success_criteria>

<output>
After completion, create `.planning/phases/03-services-page-rebuild/03-01-SUMMARY.md`
</output>
