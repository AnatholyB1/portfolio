---
wave: 2
plan_id: 03-02
phase: 3
depends_on: [03-01]
files_modified:
  - src/components/sections/ServicesHeroSection.tsx
  - src/components/sections/ProblemSection.tsx
  - src/components/sections/ApproachSection.tsx
autonomous: true
requirements: [SVC-01, SVC-02, SVC-03]

must_haves:
  truths:
    - "ServicesHeroSection renders 3-line split title (Sites, / outils, / agents.) with badge, sub, 2 CTAs, 3 meta stats"
    - "ProblemSection renders 4 pain cards with letter labels A-D using var(--warm) accent, section num 01/09"
    - "ApproachSection renders 6 benefit rows and 3 numbered cards (01-03) with section num 02/09"
    - "All three sections use data-reveal and data-reveal-delay attributes"
    - "All strings come from t.services.hero / t.services.problem / t.services.approach via useLanguage()"
    - "No hardcoded hex values — only CSS vars"
  artifacts:
    - path: "src/components/sections/ServicesHeroSection.tsx"
      provides: "Services hero with 3-line display title + meta grid"
      contains: "title_l1"
    - path: "src/components/sections/ProblemSection.tsx"
      provides: "4 pain cards with warm accent and A-D labels"
      contains: "var(--warm)"
    - path: "src/components/sections/ApproachSection.tsx"
      provides: "6 benefits + 3 numbered approach cards"
      contains: "padStart(2, '0')"
  key_links:
    - from: "ServicesHeroSection.tsx"
      to: "/#contact"
      via: "cta_audit href"
      pattern: "href=\"/#contact\""
    - from: "ProblemSection.tsx"
      to: "t.services.problem.items"
      via: "map over items array"
      pattern: "problem\\.items\\.map"
---

<objective>
Build the first three section components: ServicesHeroSection, ProblemSection, and ApproachSection.

Purpose: These are the top of the page — the first content the visitor sees after the Navbar.
Output: Three .tsx files in src/components/sections/, each self-contained with i18n and design tokens.
</objective>

<execution_context>
@C:/Users/anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-services-page-rebuild/03-CONTEXT.md
@.planning/phases/03-services-page-rebuild/03-01-SUMMARY.md

<interfaces>
<!-- Established patterns from Phase 2 — executor reads these, no exploration needed -->

From src/components/sections/Manifeste.tsx (Phase 2 pattern for section structure):
```typescript
'use client';
import { useLanguage } from '@/context/LanguageContext';

export default function Manifeste() {
  const { t } = useLanguage();
  const ts = t.landing.manifeste;
  return (
    <section className="sec border-t" id="manifeste">
      <div className="wrap">
        <div className="sec-head" data-reveal>
          <div className="sec-num">{ts.num}</div>
          <h2 className="sec-title">...</h2>
        </div>
      </div>
    </section>
  );
}
```

CSS vars available (from globals.css, Phase 1):
- var(--bg)       → #0A0B0C  (page background)
- var(--acid)     → #C4F542  (primary accent, used for active states)
- var(--ink)      → #ECEAE3  (primary text)
- var(--warm)     → #E07856  (REQUIRED for Problem section pain cards)
- var(--line)     → #1F1F1F  (borders, dividers)
- var(--bg-2)     → slightly lighter background
- var(--ink-dim)  → dimmed text
- var(--ink-faint)→ faint text

i18n access pattern (per D-07):
```typescript
const { t } = useLanguage();
const ts = t.services.hero; // namespace per section
```

Cross-page CTA href pattern (per D-10):
href="/#contact"  ← REQUIRED (not "#contact" which breaks from /services)
href="#offers"    ← internal anchor stays relative (same page)

data-reveal pattern (per D-08):
data-reveal on section wrappers, card containers, headings
data-reveal-delay="0" through "3" for stagger (string, not number)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServicesHeroSection — 3-line title, 2 CTAs, 3 meta stats</name>
  <files>src/components/sections/ServicesHeroSection.tsx</files>

  <read_first>
    - C:/Users/Anatholy/Downloads/portfolio/services-app.jsx (SHero function, lines 82-124 — exact layout reference)
    - C:/Users/Anatholy/Downloads/portfolio/03 Services.html (s-hero CSS classes for layout reference)
    - src/components/sections/Manifeste.tsx (Phase 2 pattern for 'use client', useLanguage, wrap class)
  </read_first>

  <action>
Create `src/components/sections/ServicesHeroSection.tsx` as a named export default function `ServicesHeroSection`.

Structure (from services-app.jsx SHero):
- `<section className="s-hero">` with a decorative `<div className="s-hero-grid" />` background
- Top bar (`s-hero-top`): breadcrumb link back to `/` + pill badge with dot + `{ts.badge}` text
- Hero title (`s-hero-title split`): 3 `<span className="line">` wrappers, each containing `<span>`:
  - Line 1: `{ts.title_l1}` (plain)
  - Line 2: `{ts.title_l2}` (plain)
  - Line 3: `<em className="it">{ts.title_l3_it}</em>` (italic "agents.")
- Bottom bar (`s-hero-bottom`), `data-reveal data-reveal-delay="2"`: three columns:
  - Column 1: sub text (`{ts.sub}`) + CTA buttons row
    - Primary CTA: `<a href="/#contact" className="btn btn-primary">{ts.cta_audit} <span className="ar">→</span></a>` (per D-10)
    - Secondary CTA: `<a href="#offers" className="btn btn-ghost">{ts.cta_offers}</a>`
  - Column 2: first meta stat — `<span className="k">{ts.meta[0].k}</span>` + `<span className="v">{ts.meta[0].v}</span>` inside `<div className="s-hero-meta">`
  - Column 3: second and third meta stats stacked — `ts.meta[1]` and `ts.meta[2]`, each in `<div className="s-hero-meta">`

Per D-08: add `data-reveal` to `s-hero-top` div and `data-reveal data-reveal-delay="2"` to `s-hero-bottom` div.
Per D-05: no hex values — use CSS vars and className only.
Per D-07: `const { t } = useLanguage(); const ts = t.services.hero;`
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep ServicesHeroSection</automated>
  </verify>

  <acceptance_criteria>
    - `src/components/sections/ServicesHeroSection.tsx` exists
    - File contains `'use client'`
    - File contains `useLanguage` import
    - File contains `t.services.hero`
    - File contains `title_l1` and `title_l3_it`
    - File contains `href="/#contact"` (absolute, per D-10)
    - File contains `data-reveal`
    - File contains `className="it"` for the italic line
    - File does NOT contain any hex color literals (#XXXXXX)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>

  <done>ServicesHeroSection.tsx renders 3-line split title, breadcrumb pill, 2 CTAs, 3 meta stats — no type errors</done>
</task>

<task type="auto">
  <name>Task 2: ProblemSection — 4 warm-accent pain cards with A-D labels</name>
  <files>src/components/sections/ProblemSection.tsx</files>

  <read_first>
    - C:/Users/Anatholy/Downloads/portfolio/services-app.jsx (SProblem function, lines 127-151)
    - C:/Users/Anatholy/Downloads/portfolio/03 Services.html (problem-card, pn CSS classes)
    - src/components/sections/Manifeste.tsx (sec / sec-head / sec-num pattern)
  </read_first>

  <action>
Create `src/components/sections/ProblemSection.tsx` as a named export default function `ProblemSection`.

Structure (from services-app.jsx SProblem):
- `<section className="sec">` (no border-t on first section after hero)
- `<div className="wrap">`
- Section header (`sec-head`, `data-reveal`):
  - `<div className="sec-num">{ts.num}</div>` — renders "01 / 09"
  - `<h2 className="sec-title">{ts.title_l1}<br /><em className="it">{ts.title_l2_it}</em></h2>`
  - `<p className="sec-intro">{ts.intro}</p>`
- Problem grid (`problem-grid`): map over `ts.items` (4 items):
  - `<div className="problem-card" data-reveal data-reveal-delay={String(i % 4)} key={i}>`
  - `<span className="pn">{it.n}</span>` — renders letter A, B, C, D
  - `<h3>{it.title}</h3>`
  - `<p>{it.desc}</p>`
- Good news bar: `<div className="problem-good" data-reveal>{ts.good_news}</div>`

The `pn` class (letter label) must use `var(--warm)` color — this is the warm accent requirement for SVC-02. Apply via CSS class in globals.css (Plan 6), but declare the className here so the CSS plan can target it.

Per D-07: `const ts = t.services.problem;`
Per D-05: no hex values in component — the warm color is applied via `.pn` CSS class.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep ProblemSection</automated>
  </verify>

  <acceptance_criteria>
    - `src/components/sections/ProblemSection.tsx` exists
    - File contains `'use client'`
    - File contains `t.services.problem`
    - File contains `it.n` for the letter labels
    - File contains `className="pn"` (CSS plan will apply var(--warm))
    - File contains `className="problem-card"`
    - File contains `data-reveal` on cards with `data-reveal-delay`
    - File contains `sec-num` div rendering the section number
    - File does NOT contain hex color literals
    - `npx tsc --noEmit` passes
  </acceptance_criteria>

  <done>ProblemSection.tsx renders 4 pain cards with pn letter labels, sec-num, intro, good_news — no type errors</done>
</task>

<task type="auto">
  <name>Task 3: ApproachSection — 6 benefits + 3 numbered approach cards</name>
  <files>src/components/sections/ApproachSection.tsx</files>

  <read_first>
    - C:/Users/Anatholy/Downloads/portfolio/services-app.jsx (SApproach function, lines 154-187)
    - C:/Users/Anatholy/Downloads/portfolio/03 Services.html (approach, approach-text, approach-cards, approach-card CSS)
    - src/components/sections/Manifeste.tsx (sec / wrap / sec-head pattern)
  </read_first>

  <action>
Create `src/components/sections/ApproachSection.tsx` as a named export default function `ApproachSection`.

Structure (from services-app.jsx SApproach):
- `<section className="sec border-t">`
- `<div className="wrap">`
- Section header (`sec-head`, `data-reveal`):
  - `<div className="sec-num">{ts.num}</div>` — "02 / 09"
  - `<h2 className="sec-title">{ts.title_l1}<br /><em className="it">{ts.title_l2_it}</em></h2>`
  - `<p className="sec-intro">{ts.intro}</p>`
- Two-column approach layout (`approach`):
  - Left column (`approach-text`, `data-reveal`): benefits list
    - `<div className="benefits">` → map `ts.benefits` (6 items)
    - Each: `<div className="benefit" key={i}><span className="c">→</span>{b}</div>`
  - Right column (`approach-cards`, `data-reveal data-reveal-delay="1"`): 3 numbered cards
    - Map `ts.cards` (3 items):
    - `<div className="approach-card" key={i}>`
    - `<span className="idx">{String(i + 1).padStart(2, '0')}</span>` — renders 01, 02, 03
    - `<div><h4>{c.t}</h4><p>{c.d}</p></div>`

Per D-08: data-reveal on approach-text and approach-cards.
Per D-07: `const ts = t.services.approach;`
Per D-05: no hex values.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep ApproachSection</automated>
  </verify>

  <acceptance_criteria>
    - `src/components/sections/ApproachSection.tsx` exists
    - File contains `'use client'`
    - File contains `t.services.approach`
    - File contains `padStart(2, '0')` for numbered cards
    - File contains `className="benefit"` and `className="approach-card"`
    - File contains `ts.benefits.map` and `ts.cards.map`
    - File contains `className="sec-num"` with `{ts.num}`
    - File contains `data-reveal`
    - File does NOT contain hex color literals
    - `npx tsc --noEmit` passes
  </acceptance_criteria>

  <done>ApproachSection.tsx renders 6 benefits and 3 numbered cards (01-03) with section num — no type errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translations → JSX | Static strings rendered via React — no user input at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | ServicesHeroSection mailto link | accept | Email is intentionally public — agency contact address |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All three files exist in src/components/sections/
- `grep -l "t.services" src/components/sections/ServicesHeroSection.tsx src/components/sections/ProblemSection.tsx src/components/sections/ApproachSection.tsx` returns all 3 files
- `grep "/#contact" src/components/sections/ServicesHeroSection.tsx` returns the CTA href
- `grep "var(--warm)\|pn" src/components/sections/ProblemSection.tsx` returns className reference
</verification>

<success_criteria>
- ServicesHeroSection.tsx: 3-line split title, breadcrumb, pill badge, 2 CTAs (href="/#contact" + href="#offers"), 3 meta stats grid
- ProblemSection.tsx: sec-num "01/09", 4 cards with pn letter labels (A-D), good_news bar, data-reveal stagger
- ApproachSection.tsx: sec-num "02/09", 6 benefits with arrow prefix, 3 cards with 01/02/03 numbering, data-reveal
- No hardcoded hex in any of the 3 files
- All strings from t.services.{hero|problem|approach} namespace
- `npx tsc --noEmit` clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-services-page-rebuild/03-02-SUMMARY.md`
</output>
