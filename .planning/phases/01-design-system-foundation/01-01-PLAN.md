---
phase: 01-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
autonomous: true
requirements:
  - DS-01

must_haves:
  truths:
    - "Any page renders with background color #0A0B0C — no white or gray background"
    - "CSS vars --bg, --acid, --ink, --warm, --line, --bg-2, --ink-dim, --ink-faint, --grid are declared in :root"
    - "Tailwind utilities bg-bg, text-acid, text-ink, text-warm, border-line, bg-bg-2, text-ink-dim, text-ink-faint are available via @theme inline"
    - "No legacy values remain: no #0a0a0a, no #ededed, no --background, no --foreground, no indigo scrollbar, no Inter font reference in globals.css"
    - "Scrollbar is 6px wide with #222 thumb on #000 track"
    - "Selection highlight is acid color (#C4F542) with black text"
  artifacts:
    - path: "src/app/globals.css"
      provides: "All design tokens and Tailwind v4 theme integration"
      contains: "--bg:        #0A0B0C"
  key_links:
    - from: "src/app/globals.css :root"
      to: "src/app/globals.css @theme inline"
      via: "var(--bg) references in --color-bg"
      pattern: "--color-bg:\\s*var\\(--bg\\)"
---

<objective>
Rewrite globals.css to establish all 9 design tokens, wire them into Tailwind v4's @theme inline block, replace legacy scrollbar and selection styles, and add the body base rule.

Purpose: Every page in the site inherits the Selenium Phase 02 color palette automatically. Phase 2/3 components can use Tailwind utility classes like `bg-bg`, `text-acid`, `border-line` without additional config.
Output: A rewritten globals.css with the complete token set, @theme inline integration, body rule, scrollbar, selection, and html smooth-scroll rule.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-CONTEXT.md

<interfaces>
<!-- Current globals.css — full file known, rewrite in place. -->

Current file (48 lines):
Line 1:  @import "tailwindcss";
Lines 2–6:  :root { --background: #0a0a0a; --foreground: #ededed; }
Lines 8–12: @theme inline { --color-background, --color-foreground, --font-sans: var(--font-inter) }
Lines 14–18: body { ... uses --background, --foreground, --font-sans }
Lines 20–47: html smooth-scroll, webkit scrollbar (8px, #1a1a1a track, #4f46e5 indigo thumb), ::selection (indigo)

EVERYTHING from line 2 onward is replaced. Line 1 (@import "tailwindcss") is preserved as-is.

Tailwind v4 note: This project uses Tailwind v4 via @import "tailwindcss" with no tailwind.config.js.
Token exposure MUST use the @theme inline block (not a JS config file).
Utility name mapping: --color-bg → bg-bg, --color-acid → text-acid / bg-acid, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite globals.css with design tokens, @theme inline, and base styles</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css — read the current file before overwriting (already done in planning — 48 lines, inter font ref, indigo colors)
    - C:/Users/Anatholy/Downloads/portfolio/01 Direction creative.html — lines 26–38 for body rule, scrollbar, selection reference values
    - .planning/phases/01-design-system-foundation/01-PATTERNS.md — complete new globals.css structure is spelled out in the Pattern Map
  </read_first>
  <action>
Overwrite src/app/globals.css entirely with the following content (preserve only the @import line, replace everything else):

```css
@import "tailwindcss";

/* ─── Design Tokens ──────────────────────────────── */
:root {
  /* Core (DS-01) */
  --bg:        #0A0B0C;
  --acid:      #C4F542;
  --ink:       #ECEAE3;
  --warm:      #E07856;
  --line:      #1F1F1F;

  /* Extended — used by Phase 2/3 components */
  --bg-2:      #111213;
  --ink-dim:   #9A9690;
  --ink-faint: #5A5751;
  --grid:      rgba(236,234,227,0.04);
}

/* ─── Font CSS vars (populated by next/font/google in layout.tsx) ─ */
:root {
  --font-display: var(--font-bricolage);
  --font-body:    var(--font-manrope);
  --font-mono:    var(--font-jetbrains);
}

/* ─── Tailwind v4 @theme integration ─────────────── */
@theme inline {
  --color-bg:         var(--bg);
  --color-acid:       var(--acid);
  --color-ink:        var(--ink);
  --color-warm:       var(--warm);
  --color-line:       var(--line);
  --color-bg-2:       var(--bg-2);
  --color-ink-dim:    var(--ink-dim);
  --color-ink-faint:  var(--ink-faint);
}

/* ─── Base ────────────────────────────────────────── */
html {
  scroll-behavior: smooth;
}

body {
  background: var(--bg);
  color: var(--ink);
  font-family: var(--font-body), system-ui, sans-serif;
  font-weight: 400;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
  overflow-x: hidden;
}

/* ─── Scrollbar ───────────────────────────────────── */
::-webkit-scrollbar              { width: 6px; height: 6px; }
::-webkit-scrollbar-track        { background: #000; }
::-webkit-scrollbar-thumb        { background: #222; border-radius: 3px; }
::-webkit-scrollbar-thumb:hover  { background: #333; }

/* ─── Selection ───────────────────────────────────── */
::selection { background: var(--acid); color: #000; }
```

DO NOT include:
- `--font-sans: var(--font-inter)` in @theme (Inter is gone)
- `--color-background` or `--color-foreground` (legacy vars removed)
- `#4f46e5` or `#6366f1` or `rgba(99, 102, 241` (indigo colors — all gone)
- Any `--background` or `--foreground` var references
  </action>
  <verify>
    <automated>
      cd C:/portfolio && rtk grep "--bg:" src/app/globals.css && rtk grep "color-acid" src/app/globals.css && rtk grep "font-inter" src/app/globals.css
    </automated>
    Note: grep for --bg: and color-acid must return results; grep for font-inter must return NO results.
  </verify>
  <acceptance_criteria>
    - globals.css line 1 is exactly: @import "tailwindcss";
    - globals.css contains: --bg:        #0A0B0C
    - globals.css contains: --acid:      #C4F542
    - globals.css contains: --ink:       #ECEAE3
    - globals.css contains: --warm:      #E07856
    - globals.css contains: --line:      #1F1F1F
    - globals.css contains: --bg-2:      #111213
    - globals.css contains: --ink-dim:   #9A9690
    - globals.css contains: --ink-faint: #5A5751
    - globals.css contains: --grid:      rgba(236,234,227,0.04)
    - globals.css contains: --color-bg:         var(--bg)
    - globals.css contains: --color-acid:       var(--acid)
    - globals.css contains: --color-ink:        var(--ink)
    - globals.css contains: @theme inline {
    - globals.css does NOT contain: --font-inter
    - globals.css does NOT contain: #0a0a0a
    - globals.css does NOT contain: #ededed
    - globals.css does NOT contain: #4f46e5
    - globals.css does NOT contain: rgba(99, 102, 241
    - globals.css does NOT contain: --background
    - globals.css does NOT contain: --foreground
    - globals.css contains: ::-webkit-scrollbar              { width: 6px
    - globals.css contains: background: #000
    - globals.css contains: background: #222
    - globals.css contains: ::selection { background: var(--acid); color: #000; }
    - rtk next build exits 0 (no CSS parse errors)
  </acceptance_criteria>
  <done>globals.css is fully rewritten. All 9 tokens declared in :root. All 8 color tokens exposed as --color-* in @theme inline. No legacy inter/indigo/background/foreground references remain. Scrollbar is 6px/#222/#000. Selection is acid/black.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file → browser | globals.css values are static compile-time constants, no runtime injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | globals.css CSS vars | accept | CSS vars are static strings; no user input reaches this file at any point |
| T-01-02 | Information Disclosure | Color token values | accept | Design tokens are intentionally public — no secrets embedded in CSS |
</threat_model>

<verification>
After task completion:
1. `grep -c "var(--bg)" src/app/globals.css` — should return ≥ 1
2. `grep "font-inter" src/app/globals.css` — should return nothing (exit 1 / no match)
3. `grep "#4f46e5" src/app/globals.css` — should return nothing
4. `rtk next build` — exits 0 with no TypeScript or CSS errors
</verification>

<success_criteria>
- globals.css contains all 9 :root tokens verbatim
- @theme inline block exposes all 8 color tokens (--grid excluded — not a color utility)
- Zero legacy references (inter, indigo, #0a0a0a, #ededed, --background, --foreground)
- Scrollbar: 6px width, #222 thumb, #000 track
- Selection: var(--acid) background, #000 text
- Build passes (rtk next build exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md`
</output>
