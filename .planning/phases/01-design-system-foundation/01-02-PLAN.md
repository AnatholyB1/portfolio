---
phase: 01-design-system-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
autonomous: true
requirements:
  - DS-02

must_haves:
  truths:
    - "Bricolage Grotesque, Manrope, and JetBrains Mono load on every page via next/font/google"
    - "Each font exposes a CSS variable: --font-bricolage, --font-manrope, --font-jetbrains on <body>"
    - "No Inter import or --font-inter variable remains in layout.tsx"
    - "The <body> className contains bricolage.variable, manrope.variable, and jetbrainsMono.variable"
    - "Legacy gradient classes (bg-linear-to-br, from-gray-900, via-black) are removed from body className"
    - "LanguageProvider wrapper is preserved exactly — no children restructuring"
    - "CustomCursor and CinemaIntro are NOT yet added (those are Plans 03 and 05)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Font loading with CSS variable injection"
      contains: "Bricolage_Grotesque"
  key_links:
    - from: "layout.tsx bricolage.variable"
      to: "globals.css --font-display: var(--font-bricolage)"
      via: "CSS variable inheritance on <body>"
      pattern: "variable.*font-bricolage"
---

<objective>
Update layout.tsx to load three Google Fonts (Bricolage Grotesque, Manrope, JetBrains Mono) via next/font/google, expose them as CSS variables, and replace the Inter font and legacy body classes.

Purpose: Every page uses the Selenium Phase 02 typefaces with no flash of unstyled text (FOUIT). Fonts are served locally by Next.js — no external CDN dependency (D-01).
Output: Updated layout.tsx with three font declarations, updated body className, Inter reference fully removed.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-CONTEXT.md

<interfaces>
<!-- Current layout.tsx — all relevant lines extracted -->

Line 1:  import type { Metadata } from "next";
Line 2:  import { Inter } from "next/font/google";       ← REPLACE
Line 3:  import "./globals.css";
Line 4:  import { LanguageProvider } from "@/context/LanguageContext";
Lines 6–9: const inter = Inter({ variable: "--font-inter", subsets: ["latin"] }); ← REPLACE
Lines 11–64: export const metadata = { ... }  ← DO NOT TOUCH
Lines 66–82: export default function RootLayout — body className on line 74 ← UPDATE className only

Line 74 current:
  className={`${inter.variable} font-sans antialiased bg-linear-to-br from-gray-900 via-black to-gray-900 text-white min-h-screen`}

Line 74 target:
  className={`${bricolage.variable} ${manrope.variable} ${jetbrainsMono.variable} antialiased`}

Lines 76–78: LanguageProvider wrapper — PRESERVE EXACTLY:
  <LanguageProvider>
    {children}
  </LanguageProvider>

dynamic import line (add after line 4, alongside other imports):
  import dynamic from "next/dynamic";
  (CustomCursor and CinemaIntro dynamic imports are NOT added in this plan — Plans 03/05 add them)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Inter for three Google Fonts and clean body className</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx — read the full file before editing (83 lines — already read in planning)
    - .planning/phases/01-design-system-foundation/01-CONTEXT.md — decisions D-01 through D-05 for exact font config values
    - .planning/phases/01-design-system-foundation/01-PATTERNS.md — "layout.tsx (config)" section for exact import block and font declaration shapes
  </read_first>
  <action>
Make the following targeted changes to src/app/layout.tsx:

1. REPLACE line 2 (Inter import):
   FROM: import { Inter } from "next/font/google";
   TO:   import { Bricolage_Grotesque, Manrope, JetBrains_Mono } from "next/font/google";

2. REPLACE lines 6–9 (inter font declaration):
   FROM:
     const inter = Inter({
       variable: "--font-inter",
       subsets: ["latin"],
     });
   TO:
     const bricolage = Bricolage_Grotesque({
       variable: "--font-bricolage",
       subsets: ["latin"],
       axes: ["opsz"],
     });

     const manrope = Manrope({
       variable: "--font-manrope",
       subsets: ["latin"],
       weight: ["300", "400", "500", "600", "700"],
     });

     const jetbrainsMono = JetBrains_Mono({
       variable: "--font-jetbrains",
       subsets: ["latin"],
       weight: ["300", "400", "500"],
     });

3. REPLACE body className (line 74 equivalent):
   FROM: className={`${inter.variable} font-sans antialiased bg-linear-to-br from-gray-900 via-black to-gray-900 text-white min-h-screen`}
   TO:   className={`${bricolage.variable} ${manrope.variable} ${jetbrainsMono.variable} antialiased`}

4. DO NOT CHANGE:
   - Lines 1, 3, 4 (Metadata import, globals.css import, LanguageProvider import)
   - Lines 11–64 (entire metadata export — do not touch)
   - Lines 66–73 (RootLayout function signature, html tag)
   - Lines 75–79 (LanguageProvider wrapper with {children})
   - Lines 80–82 (closing tags)

IMPORTANT: Do NOT add dynamic imports for CustomCursor or CinemaIntro in this plan. Those are added in Plans 03 and 05 respectively.
  </action>
  <verify>
    <automated>
      cd C:/portfolio && rtk grep "Bricolage_Grotesque" src/app/layout.tsx && rtk grep "font-inter" src/app/layout.tsx && rtk next build
    </automated>
    Note: grep for Bricolage_Grotesque must return a result; grep for font-inter must return NO result; next build must exit 0.
  </verify>
  <acceptance_criteria>
    - layout.tsx contains: import { Bricolage_Grotesque, Manrope, JetBrains_Mono } from "next/font/google"
    - layout.tsx does NOT contain: import { Inter }
    - layout.tsx does NOT contain: font-inter
    - layout.tsx does NOT contain: inter.variable
    - layout.tsx contains: variable: "--font-bricolage"
    - layout.tsx contains: axes: ["opsz"]
    - layout.tsx contains: variable: "--font-manrope"
    - layout.tsx contains: weight: ["300", "400", "500", "600", "700"]
    - layout.tsx contains: variable: "--font-jetbrains"
    - layout.tsx contains: weight: ["300", "400", "500"]
    - layout.tsx body className contains: ${bricolage.variable} ${manrope.variable} ${jetbrainsMono.variable}
    - layout.tsx body className does NOT contain: bg-linear-to-br
    - layout.tsx body className does NOT contain: from-gray-900
    - layout.tsx body className does NOT contain: text-white
    - layout.tsx still contains: import { LanguageProvider } from "@/context/LanguageContext"
    - layout.tsx still contains: <LanguageProvider>
    - layout.tsx metadata export (lines 11–64) is unchanged — still contains "Full Stack Developer" in title
    - rtk next build exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>layout.tsx loads three Google Fonts with correct CSS variables and axes config. body className has the three font variables and antialiased only. Inter is fully gone. Metadata and LanguageProvider are untouched. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| next/font/google → local serving | Next.js downloads fonts at build time and serves them from the local domain — no CDN dependency at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Google Fonts download | accept | next/font/google serves fonts locally at build time; no client-side request to fonts.googleapis.com at runtime (D-01 explicitly chose this) |
| T-02-02 | Denial of Service | Font build-time download | accept | Build-time network failure is a CI concern, not a runtime security threat; fonts are cached after first build |
</threat_model>

<verification>
After task completion:
1. `grep "Bricolage_Grotesque" src/app/layout.tsx` — returns import line
2. `grep "font-inter" src/app/layout.tsx` — returns nothing
3. `grep "LanguageProvider" src/app/layout.tsx` — returns both import and JSX usage
4. `rtk next build` — exits 0 with no TypeScript errors
</verification>

<success_criteria>
- Three fonts loaded via next/font/google with correct variable names, subsets, weights, and axes
- body className has exactly the three font variables + antialiased
- Inter fully removed
- LanguageProvider preserved
- Metadata export unchanged
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-02-SUMMARY.md`
</output>
