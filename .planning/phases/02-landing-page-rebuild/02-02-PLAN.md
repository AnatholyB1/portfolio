---
phase: 02-landing-page-rebuild
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: [LAND-04, LAND-08]

must_haves:
  truths:
    - "gsap package is installed and resolvable from node_modules"
    - "All 8 freelance-era component files are permanently deleted"
    - "npm run build (or next build) succeeds after deletion — no dangling import errors"
  artifacts:
    - path: "node_modules/gsap"
      provides: "GSAP 3.x for PhoneAgent ScrollTrigger"
  key_links:
    - from: "package.json"
      to: "node_modules/gsap"
      via: "npm install gsap"
      pattern: '"gsap":'
---

<objective>
Install the gsap package and delete the 8 freelance-era component files identified in D-01. These two operations are independent of each other and of all section builds, so they run in Wave 1 in parallel with plan 02-01.

Purpose: GSAP must be in node_modules before PhoneAgent.tsx (Wave 3) imports it. Deleted files must be gone before page.tsx is rewritten in Wave 4, to avoid the brief window where old imports still exist.

Output: gsap in node_modules/package.json. 8 files deleted from src/.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install gsap package</name>
  <files>package.json, package-lock.json</files>

  <read_first>
    - package.json — confirm gsap is NOT already listed before running install
  </read_first>

  <action>
Run the install command from the project root:

```bash
cd C:/portfolio && npm install gsap
```

This adds `gsap` to the `dependencies` section of `package.json` and resolves `node_modules/gsap`. Do NOT install `@gsap/react` — only the core `gsap` package is needed (D-02 uses dynamic import inside useEffect, not the React helper).

After install, verify the version that was installed:
```bash
node -e "const g = require('gsap'); console.log(g.version)"
```
  </action>

  <verify>
    <automated>cd C:/portfolio && node -e "require('gsap'); console.log('gsap ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep '"gsap"' package.json` matches 1 line showing a semver (e.g., `"gsap": "^3.x.x"`)
    - `node -e "require('gsap'); console.log('ok')"` exits 0 and prints "ok"
    - `ls node_modules/gsap/index.js` confirms the file exists
  </acceptance_criteria>

  <done>gsap is installed and importable. package.json lists it under dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Delete freelance-era component files (per D-01)</name>
  <files>
    src/components/sections/Hero.tsx,
    src/components/sections/About.tsx,
    src/components/sections/Projects.tsx,
    src/components/sections/PhoneAgentPromo.tsx,
    src/components/sections/Contact.tsx,
    src/components/three/Scene.tsx,
    src/components/three/FloatingShapes.tsx,
    src/components/three/StarkDisplay.tsx
  </files>

  <read_first>
    - src/app/page.tsx — confirm it imports the 5 section files and nothing else that should be kept
    - src/app/services/page.tsx — confirm it does NOT import any of the 8 files to be deleted (safety check)
  </read_first>

  <action>
Delete all 8 files in one operation. The research has verified these files are only imported by `src/app/page.tsx` and by each other — they are not imported by services/page.tsx or layout.tsx.

```bash
cd C:/portfolio && rm \
  src/components/sections/Hero.tsx \
  src/components/sections/About.tsx \
  src/components/sections/Projects.tsx \
  src/components/sections/PhoneAgentPromo.tsx \
  src/components/sections/Contact.tsx \
  src/components/three/Scene.tsx \
  src/components/three/FloatingShapes.tsx \
  src/components/three/StarkDisplay.tsx
```

After deletion, `src/app/page.tsx` will have broken imports — this is expected and intentional. page.tsx will be completely rewritten in Wave 4 (plan 02-05), which eliminates all these imports. Do NOT attempt to fix page.tsx in this plan.

Files to keep (do NOT delete):
- src/components/sections/PartnersBanner.tsx — imported by services/page.tsx
- src/components/ui/CustomCursor.tsx
- src/components/ui/CinemaIntro.tsx
- src/components/ui/ClientProviders.tsx
- src/hooks/useReveals.ts
- src/components/layout/Navbar.tsx (will be rebuilt in plan 02-05)
- src/components/layout/Footer.tsx (will be rebuilt in plan 02-05)
  </action>

  <verify>
    <automated>cd C:/portfolio && ls src/components/sections/ && ls src/components/three/ 2>/dev/null || echo "three dir gone"</automated>
  </verify>

  <acceptance_criteria>
    - `ls src/components/sections/Hero.tsx 2>/dev/null` returns nothing (file does not exist)
    - `ls src/components/sections/About.tsx 2>/dev/null` returns nothing
    - `ls src/components/sections/Contact.tsx 2>/dev/null` returns nothing
    - `ls src/components/three/Scene.tsx 2>/dev/null` returns nothing
    - `ls src/components/three/StarkDisplay.tsx 2>/dev/null` returns nothing
    - `ls src/components/sections/PartnersBanner.tsx` still exists (must NOT be deleted)
    - `ls src/components/ui/ClientProviders.tsx` still exists
  </acceptance_criteria>

  <done>All 8 freelance-era files are deleted. PartnersBanner.tsx, CustomCursor.tsx, CinemaIntro.tsx, ClientProviders.tsx, and useReveals.ts remain intact. page.tsx has broken imports — expected, will be resolved in Wave 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Package install introduces third-party code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | gsap npm package | accept | GSAP is a widely-used, reputable animation library; version pinned via package-lock.json |
| T-02-04 | Denial of Service | File deletion | accept | Deletion is intentional per D-01; safety verified by research (no other imports) |
</threat_model>

<verification>
After both tasks:
1. `grep '"gsap"' package.json` shows version
2. `node -e "require('gsap/ScrollTrigger'); console.log('ScrollTrigger ok')"` exits 0
3. `ls src/components/sections/` shows only PartnersBanner.tsx (all others deleted)
4. `ls src/components/three/` shows empty or directory-not-found
5. `ls src/components/ui/ClientProviders.tsx` confirms file still present
</verification>

<success_criteria>
- gsap listed in package.json dependencies with a valid semver
- All 8 target files confirmed deleted
- PartnersBanner.tsx, ClientProviders.tsx, CinemaIntro.tsx confirmed present
- No services/page.tsx imports broken (confirmed by reading the file)
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page-rebuild/02-02-SUMMARY.md`
</output>
