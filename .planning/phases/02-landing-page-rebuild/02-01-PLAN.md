---
phase: 02-landing-page-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/translations.ts
  - src/data/projects.ts
autonomous: true
requirements: [LAND-03, LAND-08]

must_haves:
  truths:
    - "src/lib/translations.ts exports a Translations interface with a `landing` key containing all 7 sub-namespaces (hero, manifeste, work, phone, partners, contact, footer)"
    - "src/lib/translations.ts `nav` key includes `manifeste` and `work` alongside existing keys — no existing nav key removed"
    - "All three locales (fr, en, th) have identical structure under `landing` — TypeScript compilation succeeds with zero errors"
    - "src/data/projects.ts exports 4 typed project objects matching D-03 and D-04 exactly"
  artifacts:
    - path: "src/lib/translations.ts"
      provides: "Extended Translations interface + fr/en/th data for all landing sections"
      contains: "landing: {"
    - path: "src/data/projects.ts"
      provides: "Typed project array consumed by Réalisations section"
      exports: ["Project", "projects"]
  key_links:
    - from: "src/lib/translations.ts"
      to: "src/context/LanguageContext.tsx"
      via: "Translations type re-export"
      pattern: "Translations"
    - from: "src/data/projects.ts"
      to: "src/components/sections/Realisations.tsx"
      via: "named import"
      pattern: "import.*projects.*from.*data/projects"
---

<objective>
Extend the i18n system with all landing-page translation keys and create the projects data file. These two files unblock every section component in Wave 2.

Purpose: Section components cannot be built until the types they consume exist. This plan provides both the TypeScript contracts (Translations interface extension) and the data (projects array).

Output: `src/lib/translations.ts` with `landing` namespace in all three locales. `src/data/projects.ts` with 4 typed project objects.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current Translations interface structure (from src/lib/translations.ts) -->
<!-- The interface has: nav, hero, about, projects, contact, footer, phoneAgent, services -->
<!-- nav currently has: home, about, services, projects, contact -->
<!-- ALL existing keys must be preserved exactly — only additions are made -->

<!-- Source of truth for all landing strings: -->
<!-- C:/Users/Anatholy/Downloads/portfolio/landing-i18n.jsx -->
<!-- Contains complete fr/en/th data for: nav, intro, hero, manifeste, work, phone, partners, contact, footer -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend translations.ts with landing namespace</name>
  <files>src/lib/translations.ts</files>

  <read_first>
    - src/lib/translations.ts — read current Translations interface and all three locale objects (lines 1–1209) before touching anything
    - C:/Users/Anatholy/Downloads/portfolio/landing-i18n.jsx — source of truth for all string values; copy verbatim
  </read_first>

  <action>
Modify `src/lib/translations.ts` in two steps:

STEP A — Extend the `Translations` interface (add after the existing `services` block, before the closing brace):

```typescript
  landing: {
    hero: {
      pill: string;
      title_l1: string;
      title_l2: string;
      title_l2_it: string;
      title_l3: string;
      sub: string;
      cta_primary: string;
      cta_secondary: string;
      stat_1_n: string; stat_1_l: string; stat_1_d: string;
      stat_2_n: string; stat_2_l: string; stat_2_d: string;
      stat_3_n: string; stat_3_l: string; stat_3_d: string;
      stat_4_n: string; stat_4_l: string; stat_4_d: string;
      canvas_l: string;
      canvas_r: string;
    };
    manifeste: {
      num: string;
      title_l1: string;
      title_l2_it: string;
      intro: string;
      p1: string;
      p2: string;
      quote: string;
      p3: string;
      p4: string;
    };
    work: {
      num: string;
      title_l1: string;
      title_l2_it: string;
      intro: string;
      items: { name: string; desc: string; tags: string[]; year: string }[];
    };
    phone: {
      num: string;
      badge: string;
      title_l1: string;
      title_l2: string;
      title_l3_it: string;
      sub: string;
      features: string[];
      cta_demo: string;
      cta_more: string;
      flow_label: string;
      flow_rec: string;
      flow_steps: { t: string; k: string; v: string }[];
    };
    partners: {
      title: string;
      items: { name: string; role: string; color: string }[];
    };
    contact: {
      num: string;
      title_l1: string;
      title_l2_it: string;
      sub: string;
      info: { k: string; v: string }[];
      form: {
        name_l: string;
        name_p: string;
        email_l: string;
        email_p: string;
        type_l: string;
        type_o: string[];
        msg_l: string;
        msg_p: string;
        submit: string;
        submitting: string;
        success: string;
        note: string;
      };
    };
    footer: {
      built: string;
      legal: string;
      rights: string;
    };
  };
```

Also extend the `nav` type inside the interface (add `manifeste: string; work: string;` alongside the existing fields — do NOT remove `home`, `about`, `services`, `projects`, `contact`).

STEP B — Add data to each locale constant (`fr`, `en`, `th`).

For `nav` in each locale, add the two new keys alongside existing ones:
- fr: `manifeste: "Manifeste"`, `work: "Réalisations"`
- en: `manifeste: "Manifesto"`, `work: "Work"`
- th: `manifeste: "แถลงการณ์"`, `work: "ผลงาน"`

For `landing` in each locale, copy the data verbatim from `C:/Users/Anatholy/Downloads/portfolio/landing-i18n.jsx`. The structure maps as follows:
- `fr.landing.hero` ← `TRANSLATIONS.fr.hero` (all fields)
- `fr.landing.manifeste` ← `TRANSLATIONS.fr.manifeste`
- `fr.landing.work` ← `TRANSLATIONS.fr.work` (including the `items` array)
- `fr.landing.phone` ← `TRANSLATIONS.fr.phone` (including `features[]` and `flow_steps[]`)
- `fr.landing.partners` ← `TRANSLATIONS.fr.partners` (including `items[]`)
- `fr.landing.contact` ← `TRANSLATIONS.fr.contact` (including `info[]` and `form{}`)
- `fr.landing.footer` ← `TRANSLATIONS.fr.footer` (keys: `built`, `legal`, `rights`)

Repeat for `en` and `th`. All values are copied verbatim from `landing-i18n.jsx`.

IMPORTANT: The existing `fr.hero`, `fr.contact`, `fr.footer` etc. are NOT removed — the new data lives under `fr.landing.*` as a separate namespace. The existing namespace keys serve the services page and current components until they are replaced in Phase 3/4.
  </action>

  <verify>
    <automated>cd C:/portfolio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `src/lib/translations.ts` contains the string `landing: {` at the interface level
    - `src/lib/translations.ts` contains `manifeste: string;` and `work: string;` inside the `nav` interface block
    - `npx tsc --noEmit` exits 0 — no TypeScript errors
    - `grep -c "landing:" src/lib/translations.ts` returns at least 4 (once per locale object + once in interface)
    - `grep "title_l2_it" src/lib/translations.ts` matches at least 3 lines (fr, en, th hero sections)
    - Existing `nav.home`, `nav.about`, `nav.projects` keys are still present in all three locale objects
  </acceptance_criteria>

  <done>Translations interface extended with `landing` namespace and `nav.manifeste`/`nav.work`. All three locales populate the new keys. TypeScript compiles with zero errors. No existing key removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/data/projects.ts</name>
  <files>src/data/projects.ts</files>

  <read_first>
    - C:/Users/Anatholy/Downloads/portfolio/landing-i18n.jsx — lines 51-56 (fr work.items) for the exact project data shape
    - src/lib/translations.ts — confirm the `landing.work.items` type shape matches what is being stored here
  </read_first>

  <action>
Create `src/data/projects.ts` as a new file. The file exports a typed `Project` interface and a `projects` constant array. The array contains structural data; `desc` and localized `tags` are resolved at render time from `t.landing.work.items[i]`.

```typescript
// src/data/projects.ts
// Static project records — names, tags, year, optional href
// desc is resolved from t.landing.work.items[i].desc at render time (per D-03)

export interface Project {
  /** Index in t.landing.work.items[] — used to retrieve i18n desc + tags */
  index: number;
  /** Canonical project name (same across all locales) */
  name: string;
  /** Display year */
  year: string;
  /** Optional URL for the project — null if no public link */
  href: string | null;
}

export const projects: Project[] = [
  {
    index: 0,
    name: 'Feuillette',
    year: '2025',
    href: '/demo/feuillette',
  },
  {
    index: 1,
    name: 'Gecko Cabane',
    year: '2024',
    href: null,
  },
  {
    index: 2,
    name: 'Les Folies Temps Danse',
    year: '2024',
    href: null,
  },
  {
    index: 3,
    name: 'Ghjulianu Codani',
    year: '2025',
    href: null,
  },
];
```

Note: `index` is used by `Realisations.tsx` to look up `t.landing.work.items[project.index]` for `desc` and `tags` in the active locale. This keeps the data file locale-agnostic (per D-03).
  </action>

  <verify>
    <automated>cd C:/portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `src/data/projects.ts` exists and exports `Project` interface and `projects` array
    - `grep "Feuillette" src/data/projects.ts` matches exactly 1 line
    - `grep "Ghjulianu Codani" src/data/projects.ts` matches exactly 1 line
    - `grep "index:" src/data/projects.ts` matches 4 lines (one per project)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>src/data/projects.ts exists with 4 typed Project objects matching D-03 and D-04. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build time → runtime | translations.ts is a static module — no dynamic input, no user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | translations.ts | accept | Contact email is intentionally public; no secrets in translation strings |
| T-02-02 | Tampering | projects.ts | accept | Static build-time data; no user input, no write path |
</threat_model>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` exits 0
2. `grep "landing:" src/lib/translations.ts` returns matches in the interface and in all 3 locale objects
3. `grep -n "manifeste:" src/lib/translations.ts | head -10` shows `manifeste` keys in nav for fr, en, th
4. `cat src/data/projects.ts` shows 4 project objects with `index`, `name`, `year`, `href`
</verification>

<success_criteria>
- TypeScript compiles with zero errors after both file modifications
- `Translations` interface has `landing` namespace with all 7 sub-keys
- `nav` interface has `manifeste` and `work` alongside all existing keys
- `src/data/projects.ts` has 4 projects matching D-04 exactly (Feuillette 2025, Gecko Cabane 2024, Les Folies Temps Danse 2024, Ghjulianu Codani 2025)
- No existing translation key removed or altered
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page-rebuild/02-01-SUMMARY.md`
</output>
