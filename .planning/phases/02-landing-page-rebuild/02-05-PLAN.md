---
phase: 02-landing-page-rebuild
plan: 05
type: execute
wave: 3
depends_on: [02-03, 02-04]
files_modified:
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
  - src/app/page.tsx
autonomous: true
requirements: [LAND-07, LAND-08]

must_haves:
  truths:
    - "Navbar renders with dark background (var(--bg)), acid accent (var(--acid)), Bricolage Grotesque labels, and fr/en/th language switcher"
    - "Navbar anchor links use absolute hrefs (href='/#manifeste', href='/#work', href='/#contact') so they work from /services"
    - "Footer renders a Wordmark block linking to /services and a footer bar with legal/rights/built text"
    - "page.tsx imports all 7 new sections + Navbar + Footer, calls useReveals(), and has no import of deleted freelance-era files"
    - "No freelance keywords appear in page metadata (no 'hire', 'freelance', 'Iron Man')"
  artifacts:
    - path: "src/components/layout/Navbar.tsx"
      provides: "Rebuilt Navbar with new design and language switcher"
      exports: ["default Navbar"]
    - path: "src/components/layout/Footer.tsx"
      provides: "Wordmark block + footer bar"
      exports: ["default Footer"]
    - path: "src/app/page.tsx"
      provides: "Complete landing page assembling all sections"
      exports: ["default Home", "metadata"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/hooks/useReveals.ts"
      via: "useReveals() call in component body"
      pattern: "useReveals"
    - from: "src/components/layout/Navbar.tsx"
      to: "src/context/LanguageContext.tsx"
      via: "useLanguage() hook"
      pattern: "useLanguage"
    - from: "src/app/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "import"
      pattern: "import HeroSection"
---

<objective>
Rebuild the Navbar, rebuild the Footer (with Wordmark), and completely rewrite page.tsx to assemble all seven sections. This wave finalizes the full page render.

Purpose: Navbar and Footer are shared with services/page.tsx — rebuild them correctly with absolute anchor hrefs. page.tsx replaces all freelance-era imports with the new section components and adds agency SEO metadata.

Output: Navbar.tsx rebuilt. Footer.tsx rebuilt. page.tsx complete. `npm run build` must pass.
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-landing-page-rebuild/02-03-SUMMARY.md
@.planning/phases/02-landing-page-rebuild/02-04-SUMMARY.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Canonical Navbar reference: landing-app.jsx lines 92-119 -->
<!-- Canonical Bottom/Footer reference: landing-app.jsx lines 454-484 -->

<!-- From src/context/LanguageContext.tsx: -->
<!-- export function useLanguage(): { t: Translations; lang: Lang; setLang: (l: Lang) => void } -->

<!-- From src/lib/translations.ts (after Wave 1): -->
<!-- t.nav.{ home, about, services, projects, contact, manifeste, work } -->
<!-- t.landing.footer.{ built, legal, rights } -->

<!-- From src/hooks/useReveals.ts (Phase 1 output): -->
<!-- export function useReveals(deps?: unknown[]): void -->
<!-- Must be called once in a 'use client' component — page.tsx qualifies -->

<!-- Services page to check after rebuild: -->
<!-- src/app/services/page.tsx imports Navbar and Footer — both are rebuilt in-place, -->
<!-- so the services page automatically uses the new versions. -->
<!-- Verify services page renders by checking for build errors. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Navbar.tsx and Footer.tsx</name>
  <files>src/components/layout/Navbar.tsx, src/components/layout/Footer.tsx</files>

  <read_first>
    - src/components/layout/Navbar.tsx — read current file in full before overwriting (note the import paths and Lang type usage to replicate)
    - src/components/layout/Footer.tsx — read current file in full before overwriting
    - C:/Users/Anatholy/Downloads/portfolio/landing-app.jsx — lines 92-119 (Nav) and lines 454-484 (Bottom/Footer)
    - src/app/services/page.tsx — confirm which exported names it imports from Navbar and Footer to avoid breaking it
  </read_first>

  <action>
OVERWRITE `src/components/layout/Navbar.tsx`:

```typescript
'use client';
import { useState, useEffect } from 'react';
import { useLanguage } from '@/context/LanguageContext';
import type { Lang } from '@/lib/translations';

const LANGS: Lang[] = ['fr', 'en', 'th'];

export default function Navbar() {
  const { t, lang, setLang } = useLanguage();
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const onScroll = () => setScrolled(window.scrollY > 40);
    window.addEventListener('scroll', onScroll, { passive: true });
    return () => window.removeEventListener('scroll', onScroll);
  }, []);

  return (
    <nav className={`nav ${scrolled ? 'scrolled' : ''}`}>
      <a href="/#top" className="nav-brand">
        <span className="glyph" />
        BRICON ANATHOLY
      </a>

      <div className="nav-links">
        {/* Absolute hrefs so anchor links work from /services (pitfall documented in research) */}
        <a href="/#manifeste">{t.nav.manifeste}</a>
        <a href="/#work">{t.nav.work}</a>
        <a href="/services">{t.nav.services}</a>
        <a href="/#contact">{t.nav.contact}</a>

        <div className="lang-switch">
          {LANGS.map((l) => (
            <button
              key={l}
              onClick={() => setLang(l)}
              className={lang === l ? 'active' : ''}
            >
              {l.toUpperCase()}
            </button>
          ))}
        </div>
      </div>
    </nav>
  );
}
```

OVERWRITE `src/components/layout/Footer.tsx`:

```typescript
'use client';
import { useLanguage } from '@/context/LanguageContext';

export default function Footer() {
  const { t } = useLanguage();
  const f = t.landing.footer;

  return (
    <>
      {/* Wordmark block — links to /services */}
      <a href="/services" className="wordmark wordmark-link">
        <div className="wrap">
          <div className="wordmark-row">
            <span>BRICON ANATHOLY</span>
            <span className="it">·</span>
            <span>services</span>
            <span className="it">·</span>
            <span>2026</span>
          </div>
        </div>
      </a>

      <footer>
        <div className="wrap foot">
          <div className="foot-brand">
            <span className="glyph" />
            BRICON ANATHOLY
          </div>
          <span className="label">{f.built}</span>
          <div className="foot-links">
            <a href="/#manifeste">Manifeste</a>
            <a href="/#work">Work</a>
            <a href="/services">Services</a>
            <a href="/#contact">Contact</a>
            <a href="/mentions-legales">{f.legal}</a>
          </div>
        </div>
      </footer>
    </>
  );
}
```
  </action>

  <verify>
    <automated>cd C:/portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "/#manifeste" src/components/layout/Navbar.tsx` matches at least 1 line (absolute href)
    - `grep "/#work" src/components/layout/Navbar.tsx` matches at least 1 line
    - `grep "lang-switch" src/components/layout/Navbar.tsx` matches 1 line (language switcher)
    - `grep "LANGS.map" src/components/layout/Navbar.tsx` matches 1 line
    - `grep "t.landing.footer" src/components/layout/Footer.tsx` matches at least 1 line
    - `grep "wordmark" src/components/layout/Footer.tsx` matches at least 1 line (Wordmark block present)
    - `grep "/mentions-legales" src/components/layout/Footer.tsx` matches 1 line
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>Navbar rebuilt with dark bg pattern, absolute anchor hrefs, acid language switcher, new nav keys (manifeste, work). Footer rebuilt with Wordmark block linking to /services and footer bar with legal link to /mentions-legales. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite page.tsx and update layout.tsx metadata</name>
  <files>src/app/page.tsx, src/app/layout.tsx</files>

  <read_first>
    - src/app/page.tsx — read current state before overwriting (confirm which imports exist to be removed)
    - src/app/layout.tsx — read lines 25-78 (metadata export) — will be updated for agency positioning
    - src/hooks/useReveals.ts — confirm the exported function signature before calling it
  </read_first>

  <action>
OVERWRITE `src/app/page.tsx` completely:

```typescript
'use client';
// page.tsx must be 'use client' because useReveals() calls IntersectionObserver (browser API)

import { useReveals } from '@/hooks/useReveals';
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';
import HeroSection from '@/components/sections/HeroSection';
import Manifeste from '@/components/sections/Manifeste';
import Realisations from '@/components/sections/Realisations';
import PhoneAgent from '@/components/sections/PhoneAgent';
import Partners from '@/components/sections/Partners';
import ContactSection from '@/components/sections/ContactSection';

export default function Home() {
  // Mount scroll-reveal system — sweeps all [data-reveal] elements across all child sections
  useReveals();

  return (
    <>
      <Navbar />
      <main>
        <HeroSection />
        <Manifeste />
        <Realisations />
        <PhoneAgent />
        <Partners />
        <ContactSection />
      </main>
      <Footer />
    </>
  );
}
```

Note: `metadata` export is removed from page.tsx because the file is now `'use client'` and Next.js App Router forbids `export const metadata` in Client Components. SEO metadata lives in `src/app/layout.tsx` only.

UPDATE `src/app/layout.tsx` — replace only the `metadata` export block (lines 25-78). Keep everything else (fonts, RootLayout) unchanged:

```typescript
export const metadata: Metadata = {
  title: "BRICON ANATHOLY | Agence digitale · Sites, outils & agents IA · Tours",
  description: "Agence digitale à Tours — nous concevons des sites web, des outils de gestion et des agents IA pour commerces, restaurants et services locaux. Devis sous 48h.",
  keywords: [
    "agence digitale Tours",
    "site web PME",
    "agent IA téléphonique",
    "phone agent",
    "VAPI",
    "création site web Tours",
    "outil de gestion PME",
    "agent vocal IA",
    "Selenium Studio",
    "BRICON ANATHOLY",
  ],
  authors: [{ name: "BRICON ANATHOLY" }],
  creator: "BRICON ANATHOLY",
  publisher: "BRICON ANATHOLY",
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
    },
  },
  openGraph: {
    title: "BRICON ANATHOLY | Agence digitale · Sites, outils & agents IA",
    description: "Sites web, outils de gestion et agents IA pour PME locales. Basé à Tours, France.",
    type: "website",
    locale: "fr_FR",
    siteName: "BRICON ANATHOLY — Agence digitale",
  },
  twitter: {
    card: "summary_large_image",
    title: "BRICON ANATHOLY | Agence digitale Tours",
    description: "Sites web, outils de gestion et agents IA pour commerces et services locaux.",
  },
  category: "technology",
};
```

After updating layout.tsx metadata, run a full build to verify no import errors remain and the page compiles correctly:

```bash
cd C:/portfolio && npm run build 2>&1 | tail -30
```
  </action>

  <verify>
    <automated>cd C:/portfolio && npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "freelance" src/app/layout.tsx` returns nothing (no freelance keywords in metadata)
    - `grep "hire" src/app/layout.tsx` returns nothing
    - `grep "'use client'" src/app/page.tsx` matches 1 line (first line)
    - `grep "useReveals" src/app/page.tsx` matches at least 2 lines (import + call)
    - `grep "HeroSection" src/app/page.tsx` matches 2 lines (import + JSX usage)
    - `grep "PhoneAgent" src/app/page.tsx` matches 2 lines (import + JSX usage)
    - `grep "Hero\|About\|Projects\|PhoneAgentPromo" src/app/page.tsx` returns nothing (deleted imports gone)
    - `grep "BRICON ANATHOLY | Agence" src/app/layout.tsx` matches 1 line (agency title)
    - `npm run build` exits 0 with no TypeScript or module resolution errors
  </acceptance_criteria>

  <done>page.tsx rewrites completely — 7 new sections, Navbar, Footer, useReveals(). No deleted-file imports. layout.tsx metadata updated to agency positioning — no freelance/hire keywords. npm run build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navbar → services/page | Navbar is shared — rebuilt Navbar now renders on /services; absolute hrefs prevent broken anchor navigation |
| page.tsx metadata → search engines | SEO metadata is public-facing; agency positioning replaces freelance signals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information Disclosure | layout.tsx metadata | mitigate | Remove all "hire", "freelance", personal skills language from keywords/title/description per LAND-08 |
| T-02-11 | Tampering | page.tsx 'use client' + metadata | accept | Next.js enforces no `export const metadata` in Client Components at build time — build will fail if violated |
</threat_model>

<verification>
After both tasks:
1. `npm run build` exits 0 — most important check
2. `grep "hire" src/app/layout.tsx` returns nothing
3. `grep "freelance" src/app/layout.tsx` returns nothing
4. `grep "PhoneAgentPromo\|About\|Hero " src/app/page.tsx` returns nothing (old imports gone)
5. `grep "/#manifeste" src/components/layout/Navbar.tsx` confirms absolute href
6. `grep "Agence digitale" src/app/layout.tsx` confirms agency title present
</verification>

<success_criteria>
- `npm run build` exits 0 — zero TypeScript errors, zero module resolution errors
- page.tsx is 'use client', calls useReveals(), imports all 7 new sections
- Navbar has absolute anchor hrefs, acid lang switcher, new nav keys
- Footer has Wordmark block to /services + footer bar with /mentions-legales
- layout.tsx metadata has no "hire", "freelance", "Iron Man" — uses French agency title
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page-rebuild/02-05-SUMMARY.md`
</output>
